import { judgment, makeLongformDoc, multiple, single } from './longform-utils.js';

export const DELIVERY_PLATFORM_CONTENT = {
  id: 'delivery-platform',
  name: '深度长文：交付与平台',
  icon: '🚢',
  desc: '沿着 CI/CD、DevOps、发布策略、请求链路、REST、Docker 和 K8s 深入交付可靠性。',
  sourceCards: ['CI-CD', 'Jenkins', 'DevOps', '发布策略', '请求-响应全链路', 'REST 与接口设计', 'Docker 与容器化', 'K8s 与容器编排'],
  docs: [
    makeLongformDoc({
      title: 'CI/CD 质量门禁：流水线不是把命令串起来',
      sourceCards: ['CI-CD', 'Jenkins', 'DevOps'],
      problem: '它解决的是代码从提交到上线之间缺少自动质量保障的问题。中高级前端要把 CI/CD 看成风险控制系统：安装、lint、类型、测试、构建、制品、扫描、预览环境、审批和发布记录共同组成门禁。',
      customerCase: '客户项目每次上线都靠人工本地 build，某次开发漏提交 lockfile，线上出现依赖差异。建立 CI 后，frozen install、typecheck、测试和 build 在合并前阻断问题，发布产物也能追溯到 commit。',
      flow: [
        '代码提交触发 CI。',
        '固定环境安装依赖，确保可复现。',
        '运行 lint、typecheck、unit/component/E2E 等分层检查。',
        '构建生产产物并保存 artifact。',
        '对 artifact 做体积、漏洞、sourcemap、许可证等检查。',
        '通过审批和发布策略进入 CD，并记录版本与回滚点。',
      ],
      keywords: [
        { term: 'pipeline', desc: '由多个阶段和任务组成的自动化流程。' },
        { term: 'quality gate', desc: '阻断风险进入主干或生产环境的检查。' },
        { term: 'artifact', desc: '构建后可发布、可追溯的制品。' },
        { term: 'preview environment', desc: '每个 PR 或分支对应的临时预览环境。' },
        { term: 'credentials', desc: '流水线访问仓库、registry、云资源的敏感凭证。' },
        { term: 'DORA metrics', desc: '衡量交付效能的指标，如部署频率、变更失败率和恢复时间。' },
      ],
      interview: 'CI/CD 的价值不是自动执行命令，而是把质量检查、构建制品、权限凭证、发布审批、版本追踪和回滚点标准化，让每次变更都能被验证、发布和恢复。',
      demo: `最小前端质量门禁：

\`\`\`yaml
jobs:
  check:
    steps:
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm build
      - uses: actions/upload-artifact@v4
        with:
          name: web-dist
          path: dist
\`\`\`

关键点是后续发布使用这份 artifact，而不是在发布阶段重新随机构建一次。`,
      diagnosis: [
        '流水线偶发失败时先区分代码问题、环境问题、网络问题和测试 flake。',
        '本地成功 CI 失败时检查 Node、包管理器、lockfile、环境变量。',
        '发布产物不可追溯时检查 artifact 和 release 记录。',
        '凭证泄漏风险时检查权限最小化、日志脱敏和有效期。',
        '测试很慢时按测试金字塔下沉，控制 E2E 数量。',
        '门禁过松时用线上事故倒推缺失检查。',
      ],
      followups: [
        '为什么发布应使用 CI artifact 而不是重新 build？',
        '质量门禁太严格影响效率怎么办？',
        'Jenkinsfile 里为什么不适合写复杂业务逻辑？',
        '流水线凭证如何做最小权限？',
      ],
      deepDive: '值得深入。CI/CD 是工程质量和交付效率的主干。深入顺序是 pipeline、artifact、质量门禁、缓存、凭证、安全扫描、预览环境和 DORA 指标。',
      references: ['GitHub Actions Workflow 文档', 'Jenkins Pipeline 文档', 'DORA Metrics 官方资料', 'OWASP CI/CD Security Cheat Sheet'],
      quiz: [
        single('在现代 CI/CD 持续交付流程中，设立“质量门禁（Quality Gate）”的核心价值在于：', ['在代码分支合并或进入发布流水线前，自动拦截风险并保障交付质量。', '通过静态检查完全替代人工的代码评审（Code Review）流程。', '在构建流水线对前端静态组件的样式及 UI 美学规范进行自动评分。', '为了缩短流水线构建时间，在打包时自动将所有测试用例跳过执行。'], 0, `💡 它解决了什么问题：
解决了软件发布过程中由于“人性的不确定性”和“人工校验疏漏”导致的带病上线风险。如果不配置自动化的质量门禁，开发者很容易因为漏跑单测、漏执行类型检查，将存在致命语法或业务逻辑漏洞的代码直接合并并发布上线。

🔍 核心原理解析（防拷打）：
1. 风险隔离：CI/CD 门禁的核心在于把Lint检查、TypeScript类型强校验、单元/组件测试、构建成功率以及安全依赖扫描，固化为一条不可绕过的合并/发布前置红线。
2. 通过与仓库托管平台的 PR 状态流转绑定，当且仅当所有门禁阶段均运行通过，才允许执行合并，实现客观的风险隔离。
3. 进一步拓展大厂面试追问：在研发日常迭代中，如果某项测试经常 flake（偶发失败），开发人员为了提效频繁申请临时绕过 CI 门禁，作为架构师应如何根治？切不可放开门禁。应当：① 立即隔离该 flake 测试到 nightly 任务，禁止其阻塞 PR 门禁；② 自动记录 trace 信息，为修复提供确凿证据；③ 流水线引入自动重试策略，但核心分支合并红线绝不退让。`),
        multiple('前端 CI 可包含？', ['frozen install', 'typecheck', 'test', 'build'], [0, 1, 2, 3], `💡 它解决了什么问题：
定义了一个合格前端项目在进入构建和交付前必须具备的“质量保障矩阵”。防止团队只在 CI 里执行简单的打包，导致大量隐藏的类型缺失、代码风格错乱或逻辑断言失败代码悄无声息地溜进生产环境。

🔍 核心原理解析（防拷打）：
1. CI 管道流程：主要包括：① 'frozen install' 锁死依赖包版本，防范环境漂移；② 'lint' 校验代码规范性与基本规约；③ 'typecheck' 运行 TypeScript 编译器，捕捉静态类型不一致；④ 'test' 运行分层测试，验证逻辑正确性；⑤ 'build' 最终输出发布制品。
2. 这些检查在相互独立的沙箱容器（如 Docker）中运行，以最快速度提供反馈。
3. 进一步拓展大厂面试追问：如何优化 CI 流水线的运行速度，防止每次 PR 合并都需要排队等待半小时？可采取多重加速优化：① 利用持久化缓存（如 actions/cache）缓存 node_modules 依赖；② 引入增量检查机制（如使用 nx 或 turbo 仅对发生改变的子模块运行检查）；③ 将 lint 和 test 任务并行拆分到不同的 CI 容器节点中执行，将反馈耗时控制在 5 分钟以内。`),
        judgment('发布阶段最好重新 build 一次，这样 artifact 是否可追溯无所谓。', 1, `💡 它解决了什么问题：
纠正了对持续交付（CD）一致性的危险认知。防止由于“构建环境不一致”、“构建时间差内依赖库更新（无 lockfile）”导致发布阶段重新打包出来的产物与 CI 阶段验证过的产物存在物理偏差，从而发生“测试环境好好的，上线却挂了”的交付灾难。

🔍 核心原理解析（防拷打）：
1. 一致性契约：持续交付（CD）的核心准则是“构建一次，到处部署（Build Once, Deploy Anywhere）”。
2. 在 CI 阶段通过门禁验证的那份代码产物被打包为唯一的“制品（Artifact）”并保存。后续的预发测试、灰度上线、全量发布，必须直接使用这一份物理制品，而绝对禁止在发布阶段重新运行 build 命令。
3. 进一步拓展大厂面试追问：如果在预发环境和生产环境之间，有些配置项（如 API 地址、CDN 路径）完全不同，但我们又只能使用同一份 Artifact，如何设计以避免重新打包？应当采用“运行期配置注入（Runtime Configuration）”或在容器启动时动态修改静态文件。例如，将 API 配置写在 window.__APP_CONFIG__ 对象中，并在部署镜像启动时，通过脚本（如 Nginx 容器入口脚本）读取当前环境的环境变量，动态写入该静态配置文件中，实现代码与配置的彻底分离。`),
        single('在 CI/CD 配置文件及流水线中管理敏感凭证（Credentials）时，应遵循的原则是：', ['赋予服务账号最小权限，并对运行输出日志中的敏感密钥进行脱敏过滤。', '为了简化流水线调试开销，直接将私钥和密钥硬编码至项目版本库中。', '在流水线抛出异常时，自动将所有的环境变量打印到控制台以方便排查。', '将流水线的系统账号密匙有效期设置为永久，以完全避免发布流程中断。'], 0, `💡 它解决了什么问题：
解决了自动化发布流水线需要访问高权限云资源、镜像库或代码源时，凭证被硬编码进代码库、或者在构建日志中被意外泄露，导致公司核心资产被黑客安全窃取的问题。

🔍 核心原理解析（防拷打）：
1. 安全底线：必须严格执行“最小权限原则”。为 CI 分配的凭证只能拥有完成其自身构建发布动作所需的最小范围权限（例如：只能写指定的制品库目录，无权删除生产环境服务）。
2. 在 CI 配置中，敏感凭证必须配置在专用的 Secret 存储区，在运行期以环境变量形式注入进程，且必须开启日志脱敏，自动识别并屏蔽日志输出中的凭证纯文本。
3. 进一步拓展大厂面试追问：在多租户或者开源项目中，如果黑客恶意提交了一个 PR，并修改了 workflow 配置文件尝试打印 secrets，如何防御？CI 平台会对外部贡献者提交的 PR 做出权限限制，默认禁止这些 PR 的流水线读取 Repository Secrets，且 workflow 文件的任何变更在首次运行时都必须强制经过仓库管理员的审核确认。`),
        multiple('在排查 CI/CD 自动化流水线偶发性构建失败（Flaky Tests）时，应优先排查哪些项？', ['异步操作的等待与超时重试策略', '测试数据库及模拟（Mock）数据隔离性', '第三方外部 API 的网络连接与兜底策略', '构建脚本在做静态分析时的代码缩进风格'], [0, 1, 2], `💡 它解决了什么问题：
解决了在流水线跑测试过程中，由于“偶尔报错、再次跑又好”的不稳定（Flaky）测试，导致团队逐渐对测试门禁失去信任，最终在真正出现发布故障时选择无视报错强行发布，造成质量堤坝全面崩溃的恶性循环。

🔍 核心原理解析（防拷打）：
1. flake 的三大根源：① 时序问题——测试假设某操作立即完成，但实际存在异步延迟，需用 auto-waiting 或显式等待而非固定 sleep；② 测试数据污染——多测试共享数据库/状态导致互相干扰，需隔离或每次重置测试数据；③ 外部服务不稳定——依赖真实第三方 API 时网络抖动或限流导致偶发失败，应通过 Mock/Stub 消除不可控依赖。
2. 治理 flake 必须建立“Flaky 测试隔离机制”，利用测试框架的 retry 捕获偶发失败，并记录 trace 信息便于复盘。
3. 进一步拓展大厂面试追问：在多容器并行跑测试时，由于数据库并发冲突导致大量的测试 flake，如何治理？应该为每一个测试并发进程（Worker）分配一个完全隔离的、具有独立命名空间（如携带 Worker ID 的临时数据库）的数据库实例。并在用例执行完毕后自动执行销毁，从物理上隔绝进程间的数据冲突。`),
        single('在持续集成流水线中，术语 Artifact（制品）通常是指：', ['构建流程中产出的、具备唯一版本标识并可追溯部署的物理静态资源包。', '团队在项目初期编写的软件产品需求文档（PRD）章节标题。', '浏览器在缓存请求数据时，由 localstorage 分配的安全临时标识。', '开发人员本地机器上未被纳入版本控制的各种临时源码文件。'], 0, `💡 它解决了什么问题：
明确了持续交付（CD）流水线中的权威物料标准。防止由于发布时文件散落、版本混淆而导致线上各个服务器节点上运行的静态资源、脚本代码不一致，造成严重的版本割裂与运行故障。

🔍 核心原理解析（防拷打）：
1. 制品定义：Artifact（构建制品）是指由 CI 门禁流水线编译、校验、构建成功后生成的最终交付产物压缩包（如 dist.tar.gz 或 Docker 镜像）。
2. 它代表了某个特定 Git Commit 经过验证后的“确定性物理产物”，包含了该版本运行所需的全部静态资源和代码。在后续的部署流程中，系统会对其进行哈希校验并直接发布，以保证线上所有节点的最终一致性。
3. 进一步拓展大厂面试追问：为了防止制品库磁盘被爆满，并且保障历史版本的制品可以回滚，如何设计制品的生命周期与追溯链？应该在制品命名中强绑定 'git-commit-hash' 和 'build-number'，以便快速回溯到对应的源码。同时，在制品托管仓库上配置保留策略：仅保留最近 30 天内构建的普通制品、以及所有生产发布历史版本的权威制品，其余自动过期清理。`),
      ],
    }),
    makeLongformDoc({
      title: '渐进式发布与回滚：上线如何快速止血',
      sourceCards: ['发布策略', 'DevOps', 'K8s 与容器编排'],
      problem: '它解决的是上线风险不可控的问题。发布不是把 dist 上传完，而是控制新版本暴露范围、监控健康指标、按策略放量，并在异常时快速暂停、降级或回滚。',
      customerCase: '一个新结算页只灰度 5% 用户，但错误率和支付转化异常。因为有灰度健康指标，系统自动暂停放量并回滚，事故没有扩散到全量用户。',
      flow: [
        '构建不可变 artifact，并记录版本。',
        '先部署到小流量或白名单人群。',
        '监控技术指标和业务指标。',
        '健康则按批次放量，不健康则暂停或回滚。',
        '回滚时确认 HTML、静态资源、接口兼容和缓存。',
        '事故后复盘门禁、监控和回滚速度。',
      ],
      keywords: [
        { term: 'canary', desc: '金丝雀发布，先让少量用户使用新版本。' },
        { term: 'blue-green', desc: '蓝绿发布，两套环境切换流量。' },
        { term: 'rolling update', desc: '滚动替换实例，常见于 K8s。' },
        { term: 'feature flag', desc: '用开关控制功能暴露范围。' },
        { term: 'SLI/SLO', desc: '服务健康指标和目标，用于判断是否继续放量。' },
        { term: 'rollback', desc: '回到上一个可用版本或关闭有问题能力。' },
      ],
      interview: '成熟发布是“渐进暴露 + 健康指标 + 自动止损 + 可回滚产物”的组合；前端还要额外处理 HTML 与 hash 资源缓存一致性、旧版本 chunk 保留和接口兼容。',
      demo: `前端发布顺序：

\`\`\`txt
上传 hash 静态资源
→ 保留旧版本资源
→ 切换 HTML 或路由配置
→ 小流量灰度
→ 观察白屏率、错误率、接口失败率、转化
→ 放量或回滚
\`\`\`

不要先删旧资源。旧 HTML 可能仍在用户浏览器或 CDN 中引用旧 chunk。`,
      diagnosis: [
        '灰度异常时先看新旧版本对照，而不是全站平均。',
        '白屏率升高时检查 JS 错误、chunk 404、资源缓存和 HTML 版本。',
        '业务指标下降时检查关键漏斗和接口错误。',
        '回滚失败时检查数据库/接口兼容、CDN 缓存和旧资源保留。',
        'Feature Flag 失控时检查 owner、过期时间和配置审计。',
        '事故复盘时关注为什么没提前发现和为什么止血慢。',
      ],
      followups: [
        '金丝雀、蓝绿、滚动发布怎么选？',
        '前端为什么需要保留历史静态资源？',
        'Feature Flag 和发布策略有什么区别？',
        '业务指标能不能作为自动回滚条件？',
      ],
      deepDive: '必须深入。发布是客户影响最大的工程环节。深入顺序是 artifact、灰度、Feature Flag、健康指标、CDN 缓存、回滚、事故复盘。',
      references: ['Kubernetes Deployment Rollout 文档', 'Google SRE SLI/SLO 资料', 'LaunchDarkly Feature Flag 资料', 'web.dev Cache 文档'],
      quiz: [
        single('在生产环境部署中，引入“渐进式发布（如金丝雀发布、灰度放量）”的核心价值是：', ['限制潜在故障的受灾范围，在发生异常时能快速回滚以确保线上高可用。', '为了延长发布周期，通过多次发布操作来降低单个打包产物的体积。', '发布上线后自动暂停所有实时日志统计，避免高错误率触发运维团队告警。', '在发布紧急热修复（Hotfix）包时，能够完全绕过前置的分支流水线测试门禁。'], 0, `💡 它解决了什么问题：
解决了“一键全量发布”带来的黑天鹅风险。如果在没有金丝雀灰度或滚动发布的情况下上线，一旦新代码中包含未曾测试出来的致命 Bug，会瞬间波及 100% 的线上真实用户，引发灾难性的系统崩溃与品牌声誉损失。

🔍 核心原理解析（防拷打）：
1. 风险暴露控制：渐进式发布的核心是通过金丝雀（Canary）灰度或蓝绿部署，先将新版本流量控制在 1%、5% 的极小范围内。
2. 随后通过收集并比对灰度组与对照组的技术指标与业务指标来评估健康度。只有当所有健康指标完全达标时，才继续放大流量，直到全量覆盖，实现故障的可控止血。
3. 进一步拓展大厂面试追问：在灰度发布期间，如何确保同一个用户在多次访问或刷新页面时，始终被路由到同一个版本（灰度版或旧版），避免出现交互体验混乱？必须在网关层使用一致性哈希或对用户 Cookie 中的 SessionID 进行 Hash 取模，将同一用户的流量固定绑定到特定的灰度或对照容器组，实现“黏性会话（Sticky Sessions）”。`),
        multiple('前端灰度健康指标可包含？', ['白屏率', 'JS 错误率', '接口失败率', '转化漏斗'], [0, 1, 2, 3], `💡 它解决了什么问题：
解决了发布过程中仅靠“看后台有没有报错”来评估新版本健康度的片面性。通过构建全方位的前端健康监控指标，能够及时捕捉到后台无感知、但用户在前端大面积遇到的“首屏白屏”、“交互卡死”或“下单阻断”等前端特有故障。

🔍 核心原理解析（防拷打）：
1. 指标维度：前端灰度健康监控必须覆盖：① 基础可用性——白屏率、JS 运行时 Crash 率；② 用户体验度——LCP 首屏延迟、INP 交互延迟长任务比例；③ 业务流转度——关键转化漏斗（如点击按钮到接口成功）。
2. 在灰度放量过程中，通过在 APM 系统中按灰度 Tag 区分指标，一旦灰度组的指标超过基准线（如错误率上升 0.5%），立刻触发报警并暂停放量。
3. 进一步拓展大厂面试追问：在大厂的高并发营销活动页面中，如果由于后端某一微服务不可用，导致灰度组的前端 JS 报错骤增，监控系统如何实现“自动灰度熔断（Auto-Rollback）”？应该在 APM 监控平台配置与 CD 流水线关联的 Webhook。当监控检测到灰度组的错误率 SLO 在持续 3 分钟内超标，APM 会自动调用流水线的 Rollback API，将灰度流量一键回滚归零，实现无人工干预的智能自愈。`),
        judgment('发布新版本前可以删除所有旧 hash 资源。', 1, `💡 它解决了什么问题：
纠正了对浏览器缓存与 CDN 分发机制的盲目认知。防止因为过早物理清理服务器上的历史 chunk 文件，导致在 CDN 缓存切换、用户未刷新页面、或者网络请求延迟等场景下，浏览器请求历史静态资源抛出 chunk 404 错误，引起用户白屏。

🔍 核心原理解析（防拷打）：
1. 版本交替阵痛期：当前端发布新版本并更新 index.html 时，老用户由于浏览器缓存、Service Worker 或者是未关闭的旧标签页，其内存中运行的依然是旧版本的 index.html。
2. 当他们点击旧页面上的某个懒加载路由时，浏览器会向服务器发起对旧 hash 命名（如 detail.[old-hash].js）的 chunk 资源请求。如果我们在发布时删除了旧资源，这部分老用户将遭遇 404 错误并导致页面死锁。
3. 进一步拓展大厂面试追问：在频繁迭代的中后台项目中，为了彻底防止老 chunk 因版本更新被覆盖或遗留导致磁盘爆炸，如何设计静态资源的灰度部署与清理策略？应该将静态资源（JS/CSS/图片）部署在独立的、无状态的云 OSS上，利用 Hash 文件名的不重复性，对历史资源做“只增不删”的持久化托管；同时仅更新和覆盖 index.html 这一个入口文件，并为 OSS 设置定时策略，自动清理 90 天前未被引用的过期历史碎片。`),
        single('在通过特性开关（Feature Flag）控制功能发布时，随着项目迭代，最核心的治理重点是：', ['明确开关的 Owner、管理其失效周期，并定期审计清理已下线的开关代码。', '对开关配置文件的字体大小和字重优先级进行全局统一配置。', '评估不同图片格式（如 WebP/PNG）在开关条件判断下的性能差异。', '限制开关在源码中的变量字符长度，以缩减最终打包产物的体积。'], 0, `💡 它解决了什么问题：
解决了在引入特性开关（Feature Flag）后，由于“只加不减”而导致代码库中残留大量过期废弃的 if/else 逻辑分支，使得代码复杂度指数级飙升、测试覆盖困难、甚至可能因为开关误触引发线上灵异故障的“技术债”问题。

🔍 核心原理解析（防拷打）：
1. 治理体系：每一个 Feature Flag 的引入都必须具备生命周期所有者（Owner）、过期截止日期以及配置审计。
2. 当一个新特性灰度完成并 100% 全量上线后，开发团队必须在一个迭代内排期，将对应的 Feature Flag 逻辑分支从代码中物理移除，回归主干代码的清爽性。
3. 进一步拓展大厂面试追问：在高并发环境下，前端组件中如果包含几十个需要从远端实时获取的 Feature Flags，如何防止由于网络延迟，导致页面在渲染时频繁出现“由于开关状态迟到带来的布局闪烁（CLS）”？应该在服务端 SSR 渲染阶段或入口 BFF 处一次性将该用户拥有的 Feature Flags 注入到 HTML 的 window 对象中；或者在客户端使用持久化缓存保存最近的状态快照，以 O(1) 的速度直接同步读取渲染，随后在后台静默刷新更新缓存。`),
        multiple('在对生产发布进行安全回滚时，团队通常需要考虑哪些资源的兼容与失效策略？', ['静态资源及 HTML 强缓存的刷新', '回滚版本与脏数据的向前兼容性', '网关及 CDN 节点缓存的阻断清理', '灰度特性开关（Feature Flag）的强制同步'], [0, 1, 2, 3], `💡 它解决了什么问题：
打破了“回滚只是把代码退回去”的跨领域盲区。它提醒交付团队，回滚是一个全链路的风险控制过程，如果处理不当，可能因为“新旧数据格式不兼容”、“静态资源被删除或被 CDN 强缓存”导致越回滚越炸。

🔍 核心原理解析（防拷打）：
1. 链路回滚要求：回滚必须做到全系统兼容。① 静态资源保留：回滚 HTML 入口时，其引用的所有 hash 静态资源必须依然物理存在于 CDN/服务器上；② 接口向后兼容（Backward Compatibility）：如果新版修改了数据库 Schema 或接口字段，在回滚到旧代码时，旧代码必须能安全读写最新的数据库格式而不断裂。
2. 因此，数据库迁移和接口设计必须遵循“写时留余、两代共存”的原则。
3. 进一步拓展大厂面试追问：如果某次生产事故必须回滚，但由于新代码已经将部分用户的订单数据以新的非兼容格式写入了数据库，此时如何安全回滚？不能盲目直接回滚。应该：① 立即在前端通过 Feature Flag 一键关闭故障入口或降级为只读模式（止血）；② 编写临时的后端补丁或数据修复脚本，将脏数据洗回兼容格式；③ 在确保数据库兼容的前提下，再执行代码回滚，最后重新灰度上线。`),
        single('当金丝雀或灰度版本在上线后监控到错误率发生非预期飙升，团队的第一优先级响应动作是：', ['立即暂停后续的灰度放量并执行快速回滚，防止故障扩散。', '忽略当前局部报警，继续全量推进发布流水线以观察更大样本。', '在网关层临时屏蔽该异常服务对应的监控告警，等待深夜自动重试。', '认为当前只是因为并发量不足产生的抖动，不采取任何排查手段。'], 0, `💡 它解决了什么问题：
确立了在生产环境发生故障时的“黄金止血法则”。解决了在系统崩溃时，开发团队依然在生产环境“尝试去Debug、看日志、改代码进行测试”，导致故障影响范围被无限拉长、带来巨大经济损失的致命痛点。

🔍 核心原理解析（防拷打）：
1. 止血优先原则：在灰度或发布期间，一旦监控告警提示错误率异常，任何团队的第一优先级动作永远是“暂停放量或立即一键回滚”。
2. 绝对禁止线上就地热修复或肉身排查。回滚能够瞬间将系统恢复到上一个 100% 稳定的已知运行状态，把对客户的影响范围限制在当前已暴露的微量流量中。
3. 进一步拓展大厂面试追问：当发现回滚后故障依然没有消失，甚至有进一步恶化的趋势，可能是什么原因？如何排查？这通常说明：① 故障并不是由本次发布的代码引入，而是由于底层共享服务（如数据库、公共网关）故障或外部第三方服务瘫痪导致；② 本次发布的代码已经向数据库写入了“不兼容的破坏性数据”，即使回滚了旧代码，旧代码在读取这些脏数据时依然会不断崩溃。此时必须启动紧急容灾预案，对脏数据进行强行隔离或快速洗数据。`),
      ],
    }),
    makeLongformDoc({
      title: '请求链路定位：fetch 之后到底慢在哪里',
      sourceCards: ['请求-响应全链路', 'REST 与接口设计'],
      problem: '它解决的是前端只看到 fetch 两端，却不知道请求在 DNS、TCP、TLS、CDN、网关、BFF、服务、数据库和响应回传中哪一段慢或失败的问题。',
      customerCase: '客户说提交慢，后端日志显示 handler 只有 120ms。前端 Performance 里总耗时 3 秒，最终发现请求在网关排队和 TLS 重连上耗时巨大。单看后端 handler 会误判。',
      flow: [
        '浏览器解析 URL。',
        'DNS 查找 IP。',
        '建立 TCP 和 TLS 连接。',
        '发送 HTTP 请求到 CDN、网关或反向代理。',
        '进入 BFF 或后端 handler。',
        '访问数据库、缓存或外部服务。',
        '响应返回浏览器并被 JS 消费和渲染。',
      ],
      keywords: [
        { term: 'DNS', desc: '域名解析阶段。' },
        { term: 'TCP/TLS', desc: '连接和安全握手阶段。' },
        { term: 'gateway', desc: '网关层，可能做鉴权、限流、路由和日志。' },
        { term: 'BFF', desc: '面向前端体验的接口聚合层。' },
        { term: 'status code', desc: 'HTTP 状态码，用于表达结果类别。' },
        { term: 'trace id', desc: '跨前后端串联请求链路的标识。' },
      ],
      interview: '一次请求慢要拆链路：浏览器排队、DNS、TCP、TLS、请求发送、网关、BFF、后端、数据库、响应下载和前端处理都可能是瓶颈；只看 fetch 总耗时或后端 handler 日志都不够。',
      demo: `前端传递 Trace ID：

\`\`\`ts
const traceId = crypto.randomUUID()

await fetch('/api/orders', {
  method: 'POST',
  headers: {
    'content-type': 'application/json',
    'x-trace-id': traceId
  },
  body: JSON.stringify(payload)
})
\`\`\`

更标准的链路追踪会使用 W3C Trace Context 的 traceparent。关键是前端、BFF、后端日志能按同一个请求关联。`,
      diagnosis: [
        '先看 Network Timing，拆 DNS、连接、等待、下载。',
        '后端 handler 快但总耗时慢时，检查网关、排队、连接复用和网络。',
        '只有部分地域慢时，检查 CDN、DNS 和跨区链路。',
        '401/403 查登录态、权限、Cookie 和网关鉴权。',
        'CORS 问题查预检请求、Origin、credentials 和响应头。',
        '需要跨系统定位时用 trace id 串联前端、BFF、后端日志。',
      ],
      followups: [
        'Timing 面板里 Waiting/TTFB 高说明什么？',
        'CORS 预检为什么会让请求多一次？',
        'Trace ID 和业务埋点有什么区别？',
        '为什么接口快但页面仍然慢？',
      ],
      deepDive: '必须深入。请求链路是前端解决客户问题的基础。深入顺序是 HTTP、浏览器 Network Timing、CORS/Cookie、网关/BFF、Trace、缓存和接口设计。',
      references: ['MDN Fetch API', 'MDN Resource Timing API', 'W3C Trace Context', 'MDN HTTP Status Codes'],
      quiz: [
        single('监控系统显示后端路由处理器处理时间仅 120ms，但客户端采集的用户平均感知耗时高达 3s，这说明：', ['瓶颈可能存在于 DNS 解析、网络建连（RTT）、代理网关排队或前端大文件解析等阶段。', '客户端的计时数据必定由于时钟偏移产生了错误，应以服务端日志为准。', '用户端的网络带宽彻底瘫痪，导致浏览器完全无法发起跨域接口请求。', '全局引入的 CSS 样式文件发生了循环渲染覆盖，阻断了 DOM 树的构造。'], 0, `💡 它解决了什么问题：
打破了前后端在性能瓶颈定位上的“扯皮局限”。帮助技术团队建立端到端的完整网络时间线拆解视角，能够精确诊断出因为网关限流排队、TCP 拥塞握手、CDN 穿透等网络环境因素导致的用户端极其缓慢。

🔍 核心原理解析（防拷打）：
1. 性能时间线拆解：后端 handler 耗时 120ms 仅仅是业务逻辑在服务器内存中的执行时间。
2. 而端到端的时间线包含了：浏览器连接排队（Queueing）、DNS 解析、TCP 握手与 TLS 安全认证（特别是弱网或无连接复用下耗时巨大）、请求到达网关的排队和路由、BFF 接口聚合耗时、最终响应报文在网络链路上的传输下载时间、以及前端收到数据后的解析渲染长任务阻塞。
3. 进一步拓展大厂面试追问：如何在前端精确捕获并上报每一次请求在各个底层网络阶段（DNS、TCP、SSL、TTFB）的详细耗时，以便向后端和运维提供证据？可以利用浏览器原生的 Resource Timing API。读取对应接口请求的 domainLookupStart、connectStart、secureConnectionStart、requestStart 和 responseStart 差值，即可还原出精确到毫秒的端到端网络时间线。`),
        multiple('请求链路可能经过？', ['DNS', 'TLS', '网关/BFF', '数据库或外部服务'], [0, 1, 2, 3], `💡 它解决了什么问题：
解决了在排查接口报错或慢查询时，技术人员仅把目光局限在“前端 fetch 成功与否”与“后端 Controller 报错与否”这两个单点上，忽略了中间庞大的代理、网关、路由层可能引入的限流、过滤、协议劫持导致请求夭折的隐患。

🔍 核心原理解析（防拷打）：
1. 链路节点流转：一个网络请求在物理层面上会经历：客户端浏览器 -> 本地网络（代理） -> DNS 服务器 -> 运营商网络 -> CDN/边缘节点（缓存命中与发送） -> 反向代理/网关（进行鉴权与限流） -> BFF 服务 -> 真正业务后端 -> 数据库/外部微服务。
2. 每一个环节的解析失败、证书失效、高负载都会直接拖挂该请求。
3. 进一步拓展大厂面试追问：如果在某些大客户的企业私有网络中，前端调用后端 API 偶尔出现随机的请求挂起或被拦截，而普通外网访问完全正常，怎么定位？通常是因为企业内网防火墙（WAF）的安全策略对特定的 HTTP 请求体或 Header 进行了深度数据包检测（DPI），误将其判别为攻击行为并执行了静默丢弃。需要用 Trace ID 协同客户网络管理员，在防火墙日志中排查该请求的过滤记录。`),
        judgment('fetch 总耗时慢时，只看业务 handler 日志就足够定位。', 1, `💡 它解决了什么问题：
纠正了技术排查中的“唯日志论”盲区。指出当真正的延迟发生在服务器外部或传输层时，依靠只统计应用层执行时间的 handler 日志，会完全漏掉网关转发延迟、SSL 握手瓶颈等性能杀手。

🔍 核心原理解析（防拷打）：
1. 耗时分流解析：业务 handler 日志是由 Web 框架在进入 Controller 时开始计算，并在返回 Response 时结束计算的。它无法感知请求在网络物理传输中的耗时。
2. 如果服务器带宽被打满导致 TCP 报文频繁重传，或者网关在分发请求时发生了连接池排队，或者客户端下载大体积的响应 JSON 极其缓慢，这些时间都属于“handler 外部开销”，只能通过网络抓包或资源计时发现。
3. 进一步拓展大厂面试追问：在不需要侵入业务代码的情况下，如何监控和测量网关层转发一个 HTTP 请求到后端所消耗的“网关开销”？应该在网关层配置打印 request_time（从接受请求到发送完响应的总时间）与 upstream_response_time（后端真实处理时间）之差。当两者差异显著变大时，说明网关转发、连接池排队或客户端网络下载成为了主要的性能瓶颈。`),
        single('在分布式微服务架构中，全链路追踪系统引入 Trace ID 的主要工程价值是：', ['在复杂的微服务调用链中关联和追踪同一个客户端发起的请求拓扑。', '协助构建工具识别无副作用文件，从而对核心逻辑执行深度摇树优化。', '在 CSS 代码分割阶段，根据 Trace ID 动态生成用于样式隔离的唯一选择器。', '作为前端应用在会话过期时的临时免密登录凭证，替代传统的 JWT 鉴权。'], 0, `💡 它解决了什么问题：
解决了微服务架构与复杂前后端分布式系统中，当用户发起一个请求报错或变慢时，技术团队需要在一万台服务器的日志中大海捞针去串联该请求的追踪困境。

🔍 核心原理解析（防拷打）：
1. 链路追踪契约：Trace ID 是由调用发起端生成的一个全局唯一的 UUID。
2. 前端在发起 Fetch 请求时，通过自定义的 HTTP Header 将该 Trace ID 随请求传递给网关。网关、BFF 以及后续的每一个下游微服务在调用和记录日志时，都必须强制将该 Trace ID 附带并透传，从而在集中式日志中心中实现一键检索出该请求在全链路上的完整流转图与耗时火焰图。
3. 进一步拓展大厂面试追问：在微服务架构中，如果前端生成的 Trace ID 因为跨域被网关直接过滤，或者下游微服务在进行异步调用时丢失了 trace 上下文，该怎么治理？① 网关必须配置 Access-Control-Allow-Headers 允许前端自定义的 Trace Header 跨域通行；② 在 Node.js BFF 层，必须利用 'AsyncLocalStorage' 或者是底层 context 机制在异步上下文中绑定 Trace ID；当向消息队列发送消息时，将 Trace ID 写入消息的 metadata 中。`),
        multiple('CORS 问题应检查？', ['Origin', '预检请求', 'credentials', '响应头'], [0, 1, 2, 3], `💡 它解决了什么问题：
解决了由于对浏览器“同源策略”和“CORS 跨域资源共享”机制认知不深，导致项目在进行前后端分离部署、跨主域调用时，频繁遇到浏览器阻断通信，引发业务瘫痪的痛点。

🔍 核心原理解析（防拷打）：
1. CORS 核心机制：CORS 是浏览器底层的安全防线，防范恶意站点非法跨域读取敏感数据。
2. 浏览器会对“非简单请求”（如含有自定义 Header 或 PUT/DELETE 动词）自动先发起一次 OPTIONS 预检请求。只有当服务端在响应头中明确返回 Access-Control-Allow-Origin（匹配发起方域）、Access-Control-Allow-Methods、Access-Control-Allow-Headers，并正确响应 200/204 状态码时，浏览器才会真正发送第二个业务请求。
3. 进一步拓展大厂面试追问：如果在跨域请求中，前端需要携带用户 Cookie，CORS 的配置要求会有什么严苛的变化？① 前端 fetch 请求必须显式设置 'credentials: include'；② 服务端响应头 Access-Control-Allow-Origin 绝对不能配置为通配符 '*'，必须指定为精确的客户端 Origin 域名；③ 服务端响应头必须加上 'Access-Control-Allow-Credentials: true'。三者缺一不可，否则浏览器会强行拦截该响应。`),
        single('当业务监控显示仅某些特定地理区域（如海外或偏远地区）的用户访问卡顿，应优先检查：', ['CDN 节点覆盖及命中率、地域性 DNS 解析时延以及跨区网络传输链路质量。', '应用内各个基础交互按钮组件在不同像素分辨率下的渲染消耗。', '前端开发脚手架中 TypeScript 泛型推导在大规模组件编译下的耗时。', '项目根目录下 README.md 文档的静态分发路径是否正确指向源站。'], 0, `💡 它解决了什么问题：
定位并解决“研发本地访问快，但特定省份/偏远地域用户反馈系统卡死”的地理差异性网络痛点。防止无脑优化代码，聚焦于 CDN 分发、DNS 智能路由等网络边缘优化。

🔍 核心原理解析（防拷打）：
1. 地理网络开销：当出现局部地域慢时，通常是由于：① DNS 服务商在解析该地域的请求时，未能将用户正确路由到最近的 IP，导致发生跨网、跨物理地域延迟；② CDN 在该地域的边缘节点缓存失效，或者该节点的网络发生抖动瘫痪，导致请求不得不穿透回遥远的源站获取资源。
2. 应当利用智能 DNS、CDN 节点健康审计来优化。
3. 进一步拓展大厂面试追问：在没有大资金购买顶级 CDN 的情况下，如何通过架构手段来诊断和减轻这种偏远地域静态资源加载缓慢的问题？可以利用“Service Worker 离线预缓存”将关键的 JS/CSS 静态资源文件在用户首次访问时直接持久化缓存到本地浏览器的 Cache Storage 中。在后续迭代版本更新前，页面渲染均直接读取本地 Cache，实现 0 物理网络延迟的本地加速，彻底规避偏远地域的网络波动。`),
      ],
    }),
    makeLongformDoc({
      title: 'REST 契约演进：接口怎么改才不炸前端',
      sourceCards: ['REST 与接口设计', '请求-响应全链路'],
      problem: '它解决的是接口字段、错误结构、分页和版本策略随意变化导致前端大面积故障的问题。REST 不只是路径风格，它是把资源、方法、状态码、响应体、错误结构和版本演进变成稳定契约。',
      customerCase: '后端新增订单状态 REFUNDING，前端状态机没有默认分支，订单列表出现空白。接口没有破坏字段，但枚举扩展仍然破坏了前端假设。',
      flow: [
        '把 URL 设计成资源定位，而不是动词命令。',
        '用 HTTP method 表达操作语义。',
        '用 status code 表达结果类别。',
        '统一错误结构，便于客户端封装。',
        '列表分页、排序、筛选协议保持稳定。',
        '破坏性变更走版本、兼容期、迁移说明和消费方影响评估。',
      ],
      keywords: [
        { term: 'resource', desc: 'REST 中 URL 代表资源。' },
        { term: 'method semantics', desc: 'GET、POST、PUT、PATCH、DELETE 的操作语义。' },
        { term: 'status code', desc: '用协议层结果分类表达成功、客户端错误和服务端错误。' },
        { term: 'error shape', desc: '统一错误响应结构。' },
        { term: 'pagination', desc: '分页协议，可基于页码或游标。' },
        { term: 'versioning', desc: '接口版本化和兼容策略。' },
      ],
      interview: 'REST 契约的核心是用 URL 表达资源、method 表达操作、status code 表达结果、body 表达载荷，并配合统一错误结构、稳定分页协议和版本演进策略，避免接口变化靠口头同步。',
      demo: `统一错误结构：

\`\`\`json
{
  "error": {
    "code": "ORDER_NOT_FOUND",
    "message": "订单不存在",
    "requestId": "req_123"
  }
}
\`\`\`

不要所有失败都返回 200 + code。客户端需要通过 HTTP 状态码先判断结果类别，再读业务错误。`,
      diagnosis: [
        '字段缺失时检查是否是破坏性变更或可选字段未处理。',
        '枚举新增时检查前端是否有默认分支和监控。',
        '错误处理混乱时检查状态码和错误结构是否统一。',
        '分页错乱时检查游标、排序字段和数据一致性。',
        '缓存异常时检查 method 语义和 Cache-Control。',
        '版本升级时检查兼容期、迁移文档和消费方列表。',
      ],
      followups: [
        '为什么 200 + code 不是好的错误设计？',
        '游标分页和页码分页怎么取舍？',
        '新增字段和删除字段哪个更危险？',
        '枚举新增算不算兼容变更？',
      ],
      deepDive: '值得深入。接口契约是前后端协作最常见的故障源。深入顺序是 HTTP 语义、REST 资源建模、错误结构、分页、版本化、OpenAPI 和契约测试。',
      references: ['MDN HTTP Methods', 'MDN HTTP Status Codes', 'OpenAPI Specification', 'Microsoft REST API Guidelines'],
      quiz: [
        single('在设计 RESTful 风格的 API 接口时，URL 的主体设计理念更倾向于表达：', ['具体的业务资源（Noun），操作资源的逻辑由 HTTP Method 表达。', '用于调用后台执行操作的随机动词前缀（Verb）。', '在前端页面上用于渲染对应 API 数据状态的按钮样式与颜色主题。', '开发框架在进行依赖注入时所绑定的服务或组件物理命名。'], 0, `💡 它解决了什么问题：
解决了前后端接口设计无序、混乱的工程通点。如果不使用 REST 规范来约束 URL 的模型定义，团队会随手写出如 /getUser、/deleteUser、/createUser 等大量包含动词命令的混乱路径，使得接口极难维护与标准化。

🔍 核心原理解析（防拷打）：
1. 统一接口约束：在 REST 架构风格中，URL 应该代表一个唯一的、无状态的“资源定位”（使用名词复数，如 '/users' 或 '/orders'）。
2. 而具体的“操作动作”应当交由 HTTP 原生的 Method 语义来承载：GET 代表读取、POST 代表新建、PUT 代表幂等覆盖更新、PATCH 代表局部增量更新、DELETE 代表物理或逻辑删除。通过包装，构建清晰的契约范式。
3. 进一步拓展大厂面试追问：在实际高并发业务场景中，由于 HTTP DELETE 请求在某些防火墙或低端网关设备中会被直接拦截禁止通行，如何在不破坏 RESTful 设计原则的前提下解决这一兼容问题？可以在前端发送 POST 请求，但在 Header 中附加特定的覆盖指令（如 'X-HTTP-Method-Override: DELETE'），网关层拦截到该 Header 后将其重写为 DELETE 协议再分发给后端微服务，实现网关层的兼容转换。`),
        multiple('稳定接口契约应包含？', ['请求参数', '响应字段', '错误结构', '分页协议'], [0, 1, 2, 3], `💡 它解决了什么问题：
解决了在快速迭代的敏捷开发团队中，由于缺乏统一、清晰的机器可读契约，导致前后端开发只能依赖口头沟通或经常过期的 Wiki 文档进行字段比对，从而频繁产生“接口字段拼写错误”、“错误的数据类型解析崩溃”等上线事故的痛点。

🔍 核心原理解析（防拷打）：
1. 契约的完备性：一个真正能作为契约的资产必须覆盖：① 请求路径与 HTTP 动词；② 包含类型约束、是否必填的请求参数（Query/Body）；③ 成功状态下的响应体 JSON Schema；④ 统一的错误结构；⑤ 分页与排序的通用协议。
2. 推荐使用 OpenAPI（Swagger）规范描述并输出为 machine-readable 契约。
3. 进一步拓展大厂面试追问：如何在 CI 中自动发现并拦截某次代码提交所引入的“破坏性接口变更（Breaking Change）”？可以使用 OpenAPI Diff 工具。将分支上最新生成的 swagger.json 与主干分支的 swagger.json 进行 AST diff 比对。如果发现删除了已有字段、将必填参数改为非必填、或修改了字段类型，直接阻断构建流水线，并要求开发者升级主版本号，从源头上杜绝“悄悄修改接口炸前端”的现象。`),
        judgment('删除已有响应字段通常是安全兼容变更。', 1, `💡 它解决了什么问题：
纠正了对接口演进中“兼容性”的致命误判。警告开发人员绝对禁止在没有完成客户端消费评估前，私自删除已有响应字段，避免导致线上处于运行状态的各版本客户端（如老版本 App 或者是未刷新的浏览器页面）读取 undefined 并触发运行时 crash。

🔍 核心原理解析（防拷打）：
1. 向下兼容性模型：在契约演进中，接口的修改只有“向后兼容（Backward Compatible）”才是安全的。
2. 新增响应字段是兼容的，因为旧客户端会直接忽略不认识的字段；但删除已有字段、或修改已有字段的类型，对于正在依赖该字段渲染的客户端来说，属于典型的“破坏性变更（Breaking Change）”，会直接导致 JS 执行抛错。
3. 进一步拓展大厂面试追问：如果由于业务重构，某个接口响应字段确实必须废弃，如何设计安全废弃与迁移路线？应该采用“声明废弃（Deprecation）与渐进下线”三步走策略：① 首先在接口契约中将该字段标记为 'deprecated'，在 HTTP 响应头添加 Deprecation 警告，通知消费方限期迁移；② BFF 层做适配层中转，保留该字段的 dummy 空值防止客户端报错；③ 监控分析该字段的流量降为零后，才能在下一个大版本发布中物理删除该字段。`),
        single('开发规范中，若不加区别地对所有接口错误（如鉴权失败、未找到资源）都返回 HTTP 200 状态码，最主要的隐患是：', ['破坏了 HTTP 协议的标准语义，导致网关层无法依据状态码告警，且客户端请求封装和拦截逻辑困难。', '会导致接口数据完全暴露给网络嗅探工具，从而降低了整个系统的网络安全性。', '会导致 CDN 缓存节点错误地将接口的临时报错数据当作合法结果进行强缓存。', '在打包构建时，会导致编译器无法有效判断 API 响应结构并增加包体积。'], 0, `💡 它解决了什么问题：
解决了在大中型前端工程中，如果所有失败接口都返回 '200 OK + {success: false, code: 500}' 导致的异常拦截器与网关监控“集体失效”的痛点。避免了用业务码掩盖网络与系统错误的低效设计。

🔍 核心原理解析（防拷打）：
1. 协议层隔离：HTTP 状态码是网络协议层用于标识结果类别的标准机制。
2. 如果一律返回 200，会导致浏览器、负载均衡器（ALB）、CDN 边缘节点以及监控系统误认为该请求是 100% 成功的正常通信，从而无法自动生成 SLA 错误率告警，且 CDN 会错误缓存这些报错的页面。前端也无法简单地利用拦截器进行统一的协议层报错拦截。
3. 进一步拓展大厂面试追问：在某些老旧网关或跨端 WebView 中，一旦接口返回非 200 状态码，系统层会自动拦截并直接弹出系统默认的难看报错弹窗，阻止了前端业务代码读取 response body 中的具体错误码，此时如何设计？如果物理容器强制拦截了非 200 响应，只能折中使用“200 兜底 + 内部 Error Payload”，但必须配置特殊的 x-http-status 响应头，以便前端在业务拦截器中解析出模拟的真实状态码并自行路由。`),
        multiple('在公共组件库或后台 API 中新增枚举字段（Enum）时，如果业务端未同步适配，最可能导致的工程风险是？', ['状态机匹配逻辑遗漏及数据状态失控', '前端界面对应的国际化多语言文案缺失', '条件渲染分支异常导致页面交互阻断', 'TypeScript 类型系统的穷尽性检查失效'], [0, 1, 2], `💡 它解决了什么问题：
解决了在分布式契约中，“单方面扩展数据字典”引起的前端代码崩解风险。防止后端在数据库中新增了一个合法的状态枚举值，而前端因没有设计兜底处理逻辑，导致页面在遭遇新枚举时渲染出空白或未定义的“undefined”文本。

🔍 核心原理解析（防拷打）：
1. 状态机脆弱性：当前端根据接口枚举渲染状态文案时，通常会使用字典映射（如 statusText[status]）。
2. 如果后端新增了一个枚举（如 status: 'REFUNDING'），前端如果缺乏兜底处理分支，直接调用 statusText['REFUNDING'] 就会返回 undefined，导致渲染层抛出 TypeError 或界面留白。
3. 进一步拓展大厂面试追问：如何在 TypeScript 类型层面强制约束开发人员必须为所有可能新增的枚举设计兜底的分支处理，防止手漏？可以使用 TypeScript 的 'never' 类型进行“穷尽性检查（Exhaustiveness Check）”。在 switch/case 语句中，在 default 分支中把 status 赋值给一个 never 类型的变量。一旦后端契约更新了类型且前端未在新 case 中匹配它，编译期就会立刻报错，在编译阶段杜绝分支遗漏。`),
        single('当后端微服务需要发布破坏性接口变更（Breaking Change）时，团队必须提供的保障是：', ['发布明确的版本控制策略（Version）并提供双版本并存的兼容过渡期与迁移说明。', '利用夜间低谷期进行静默上线发布，避免白天流量高峰触发告警。', '在发布前只向相关业务团队的前端开发负责人进行临时的口头通知。', '发布后立即强制下线所有旧路由，以此倒逼业务端在几分钟内完成更新。'], 0, `💡 它解决了什么问题：
确保了在大团队或跨组织协作中，当接口必须进行重大重构（破坏性变更）时，系统演进能处于“平滑过渡、有据可查、不中断业务”的可控状态。

🔍 核心原理解析（防拷打）：
1. 版本化演进机制：任何破坏性变更都不应当悄无声息地上线。必须通过：① URL 版本化（如 '/v1/' 到 '/v2/'）或 Accept Header 版本化提供两条物理并存的路由通道；② 提供详尽的迁移指南说明字段的替换关系。
2. 并为旧版本接口设定明确的下线退役时间表，给消费方留下充裕的改造缓冲期。
3. 进一步拓展大厂面试追问：如果微服务中上游的 BFF 和下游的十几个消费者应用同时处于不同团队的迭代中，如何确保在发布破坏性 v2 接口时，不影响旧的 v1 消费者正常工作？应该采用“双版本双引擎部署”。微服务网关层根据客户端请求携带的版本标识，将流量分发到部署了不同版本代码的 Pod 容器组。在旧版本流量降到零之前，旧 Pod 不执行下线，保证链路完全平滑。`),
      ],
    }),
    makeLongformDoc({
      title: '容器与 K8s 运行问题：前端服务为什么间歇 502',
      sourceCards: ['Docker 与容器化', 'K8s 与容器编排', 'DevOps'],
      problem: '它解决的是前端进入 BFF、SSR、Node 服务后，必须理解线上运行环境的问题。间歇 502、Pod 重启、滚动发布失败、探针误判、资源不足和配置差异，都不是只看页面代码能解决的。',
      customerCase: 'SSR 服务促销时大量 502。业务代码没报错，但 K8s 事件显示 Pod 因内存超限被 OOMKilled，readiness 还没就绪就接流量。修复需要资源配置、探针、缓存和扩容策略一起改。',
      flow: [
        '应用被构建成镜像。',
        'Deployment 声明期望副本数和更新策略。',
        'K8s 调度 Pod 到节点。',
        'readiness 决定 Pod 是否接流量。',
        'liveness 决定异常时是否重启。',
        'Service 和 Ingress 暴露访问入口。',
        '监控资源、重启、延迟和错误率，并支持滚动更新和回滚。',
      ],
      keywords: [
        { term: 'image', desc: '包含应用和运行依赖的可分发包。' },
        { term: 'container', desc: '镜像的运行实例。' },
        { term: 'Pod', desc: 'K8s 调度的基本单元。' },
        { term: 'readiness probe', desc: '判断实例是否可以接收流量。' },
        { term: 'liveness probe', desc: '判断实例是否需要重启。' },
        { term: 'requests/limits', desc: '资源申请和限制，影响调度与稳定性。' },
      ],
      interview: '当前端负责 BFF 或 SSR 时，必须理解容器和 K8s：镜像、Pod、Deployment、Service、Ingress、探针、资源限制和滚动发布共同决定服务是否稳定；502 要从网关、Pod 状态、探针、重启和资源指标排查。',
      demo: `最小 readiness/liveness 思路：

\`\`\`yaml
readinessProbe:
  httpGet:
    path: /healthz
    port: 3000
  initialDelaySeconds: 5
  periodSeconds: 10
livenessProbe:
  httpGet:
    path: /livez
    port: 3000
  initialDelaySeconds: 30
  periodSeconds: 20
\`\`\`

readiness 和 liveness 不应随便共用一个复杂接口。readiness 关注能否接流量，liveness 关注是否需要重启。`,
      diagnosis: [
        '间歇 502 先看网关日志和 Pod 是否 ready。',
        '服务重启看 Pod events、exit code、OOMKilled 和 liveness probe。',
        '发布期间异常看 Deployment rollout、maxUnavailable、maxSurge。',
        '高峰变慢看 CPU、Memory、event loop delay、下游耗时。',
        '配置不一致看 ConfigMap、Secret、环境变量和镜像 tag。',
        '回滚无效时检查接口兼容、数据库迁移和缓存状态。',
      ],
      followups: [
        'readiness 和 liveness 为什么不能混用？',
        'requests 和 limits 配置不当会造成什么？',
        '滚动发布为什么需要 maxUnavailable/maxSurge？',
        'SSR 服务扩容为什么不只看 Pod 数量？',
      ],
      deepDive: '值得深入。中高级前端如果负责 Node/BFF/SSR，就必须能和平台、后端一起定位运行问题。深入顺序是 Dockerfile、镜像、Pod、Deployment、Service、Ingress、探针、资源和 rollout。',
      references: ['Docker 官方文档', 'Kubernetes Deployment 文档', 'Kubernetes Probes 文档', 'Kubernetes Resource Management 文档'],
      quiz: [
        single('在容器化部署（如 Kubernetes）中，配置 Readiness Probe（就绪探针）的最主要作用是：', ['决定该 Pod 实例是否已经准备好接收外部流量并开始服务请求。', '检测容器内运行的应用在发布完成后，是否应该物理删除本地临时源码。', '监测页面静态文件在加载失败时，是否能够自动触发宿主网关重新建连。', '作为数据字典约束条件，辅助系统自动识别后端的 API 字段命名格式。'], 0, `💡 它解决了什么问题：
解决了在容器化滚动发布时，由于新 Pod 还在进行数据库连接、内存预热等冷启动，就立刻被网关塞入海量流量，导致这一批冷启动请求全部超时挂死、用户大面积遇到 502/504 的发布崩溃问题。

🔍 核心原理解析（防拷打）：
1. 探针隔离职责：Readiness Probe（就绪探针）是 K8s 判定 Pod 是否可以安全对外接收流量的指示灯。
2. 只有当探针探测（如访问 Pod 的 '/healthz' 接口）返回成功（2xx-3xx 状态码）后，K8s 才会把该 Pod 的 IP 绑定到 Service 的 Endpoints 列表中，允许网关分发流量。如果在冷启动期间未通过，Pod 会保持静默。
3. 进一步拓展大厂面试追问：如果 Readiness 探针设计的取数逻辑极其复杂，会带来什么安全隐患？这会导致“故障连锁雪崩”。一旦数据库在高负载下响应变慢，所有 Pod 的 Readiness 探针都会因为超时而判定失败，K8s 会直接将所有正常运行的 Pod 一键移出流量池，导致服务瞬间彻底瘫痪。因此，Readiness 探针只应检查本地服务的轻量级健康度。`),
        multiple('间歇 502 可排查？', ['Pod readiness', '网关日志', 'Pod 重启事件', '资源限制'], [0, 1, 2, 3], `💡 它解决了什么问题：
定义了微服务与容器架构下“服务偶发性失联（502）”的核心排查链路。解决了网络运维与Node/BFF工程师仅盯着单体代码排查不出“容器实例被系统强杀”、“探针误判定”等环境引起的系统漂移问题。

🔍 核心原理解析（防拷打）：
1. 502 根因排查：HTTP 502 Bad Gateway 意味着作为代理的网关无法与后端真实容器建立连接。
2. 排查主线应包括：① 实例重启事件（Pod Events）——查看是否由于内存溢出触发 OOMKilled；② 探针误判（Readiness/Liveness Probe）——检查探针是否配置过激，在 Node 单线程发生 Long Task 阻塞时误判定超时并将实例重启；③ 资源限制（Limits）——高负载下 CPU 被限流导致连接排队溢出。
3. 进一步拓展大厂面试追问：在 Node.js 服务高负载单线程事件循环阻塞期间，如何防止 Readiness 探针因为超时导致容器被 K8s 误判并物理踢出流量池？应当将探针的 /health 检测接口进行精简，绕过大体积 CPU 渲染计算；同时利用 Node.js 的 process.hrtime() 自制事件循环阻塞率监控，在健康探针中快速返回当前主线程卡顿状态，向网关进行温和的限流降级，而不是直接导致超时被杀。`),
        judgment('前端只要负责 BFF/SSR，就完全不需要了解容器和 K8s。', 1, `💡 它解决了什么问题：
打破了前端开发人员的“技能孤岛”幻想。警告负责 BFF 或 SSR 渲染的开发人员，如果不懂容器和 K8s，线上发生容器崩溃、OOM 内存死锁、网络超时等环境问题时，将彻底丧失与运维/平台对话的能力。

🔍 核心原理解析（防拷打）：
1. 技能边界扩展：BFF和 SSR服务运行在独立的 Node.js 物理容器中。
2. 它们是线上流量的首个冲击点。Node 端的内存管理、单线程垃圾回收机制、与 K8s 调度限制密切相关。如果不理解这些，开发人员将无法解释为什么在高并发下 Node 进程会因为 V8 内存限制被 K8s OOMKilled 强杀，也无法合理配置服务的伸缩容（HPA）阈值。
3. 进一步拓展大厂面试追问：在 Node.js BFF 架构中，如果由于某次代码修改引入了隐蔽的内存泄漏，在高并发下，K8s 容器配置的 Limits 内存上限会如何应对？K8s 的 Cgroups 机制会监控到该 Pod 物理内存占用超限，宿主机内核会直接向容器派发 SIGKILL 信号执行物理强杀并触发 Pod Events 中的 OOMKilled，Node 端将无法执行任何 beforeExit 钩子。`),
        single('当 Kubernetes 监控中出现某个 Pod 发生 OOMKilled 报警时，这通常代表：', ['容器内进程使用的物理内存超出了资源限额（Limits），被宿主机内核强制终止。', '前端页面中引入的单张背景图片物理像素尺寸超限导致浏览器崩溃。', '用户端网络会话凭证（Cookie / Session）由于超出有效期被网关拒绝。', '静态代码分析工具（ESLint）在编译打包阶段检测出无法解析的类型冲突错误。'], 0, `💡 它解决了什么问题：
指明了生产环境容器“灵异消失、瞬间死亡”的物理根本原因。防范团队在内存超配或内存泄露时，误认为是代码产生了 Exception，导致排查方向跑偏。

🔍 核心原理解析（防拷打）：
1. OOM 强杀原理：OOMKilled（Out of Memory Killed）是宿主机 Linux 内核在系统检测到某容器内存占用超出了 K8s 配置文件中声明的 'resources.limits.memory' 硬性限制上限时，触发的内核保护机制。
2. 内核会立即向该 Pod 中的主进程发送 SIGKILL 信号（Exit Code 137），无需经过 Node 进程的任何垃圾回收或妥协，瞬间将其物理杀死以保护整台宿主机。
3. 进一步拓展大厂面试追问：如果在 Node.js 镜像中，配置了 K8s Limits 内存为 4GB，但是 Node 进程在内存占到 1.5GB 左右时就频繁发生 Crash 重启，且没有触发 K8s OOMKilled，这是什么原因？这通常是因为 V8 引擎默认的堆内存限制在 64 位系统下默认为 1.4GB 左右。即使容器给了 4GB 内存，Node 进程自身在堆内存用满后就会触发内部的 Out of Memory 并自杀。此时必须在容器启动命令中添加 '--max-old-space-size=3072'。`),
        multiple('在配置 Kubernetes 的 Deployment 制品发布策略时，运维或前端架构通常需要关注的核心指标与操作包括？', ['设定服务副本数（Replicas）以保证高可用与并发负载', '配置滚动更新（Rolling Update）的就绪度与存活度探针', '保障在发生崩溃时的一键回滚（Rollback）与流量降级', '客户端浏览器加载和渲染 DOM 树的解析耗时表现'], [0, 1, 2], `💡 它解决了什么问题：
解决了在云原生环境下，如何以“声明式”方式安全管理应用的期望副本数、无缝执行平滑的滚动发布、并在发生故障时快速回滚，从而避免手动拉起容器导致的版本混乱与停机断网。

🔍 核心原理解析（防拷打）：
1. 声明式控制器：Deployment 是 K8s 中用于管理无状态 Pod 副本集的控制器。
2. 开发人员仅需在 YAML 配置文件中“声明”我期望的副本数和滚动更新策略（MaxSurge / MaxUnavailable）。Deployment 会自动协调宿主机，创建新 Pod 并在就绪后下线旧 Pod，确保整个升级过程中始终有可用容器对外提供服务。
3. 进一步拓展大厂面试追问：在 Deployment 滚动更新期间，如果新版本 Pod 频繁发生 Crash 回环（CrashLoopBackOff），Deployment 是如何阻止错误的 Pod 蔓延从而保住老版本服务的？Deployment 会结合 Readiness Probe。因为新 Pod 始终无法就绪，滚动更新会被卡在第一个批次，不会继续杀死剩余的老 Pod，从而保住了生产环境大盘的安全。`),
        single('在 Kubernetes 中，如果将 Liveness Probe（存活探针）的超时或健康度阈值配置得过于苛刻，最容易引发的隐患是：', ['容器在面临临时高并发高负载导致响应慢时，被集群误判并频繁强制重启。', '会导致前端打包流水线生成的生产代码物理体积意外缩减。', '导致客户端向代理网关发送数据请求时，接口拦截并强制缓存非预期数据。', '会破坏局域网内的 DNS 映射关系，从而加快域名解析在开发机上的响应速度。'], 0, `💡 它解决了什么问题：
解决了由于对健康检查探针灵敏度控制不当，导致高负载下的健康实例频繁被 K8s“误杀并陷入无限重启死循环”的运维灾难。

🔍 核心原理解析（防拷打）：
1. 误重启陷阱：Liveness Probe（存活探针）是 K8s 判定 Pod 是否需要重启的硬性指标。
2. 如果配置过激，当 Node.js 服务因为垃圾回收、Long Task 或瞬间并发冲高发生短暂的主线程卡顿超过探针时间时，探针请求就会超时失败。K8s 会直接判定该实例已死锁，并立即强制杀掉该 Pod 重启。这会导致在高负载期间，所有容器被轮流强杀重启，服务彻底崩溃。
3. 进一步拓展大厂面试追问：如何优雅设计 Liveness 探针，既能及时捕获 Node 服务真死锁，又不会产生误判？① 将 Liveness 的检测频率和失败阈值放宽；② Liveness 探针接口只做极度轻量的指标判断，不执行任何 I/O 操作；③ 确保 initialDelaySeconds 大于服务冷启动的预热时间。`),
      ],
    }),
  ],
};
