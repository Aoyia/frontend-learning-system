export const TENCENT_FOLLOWUP_ANSWERS = {
  // 1. 框架源码与运行机制
  '如果页面频繁重复渲染，你怎么从响应式依赖和调度器角度定位？': {
    brief: '定位频繁重渲染，核心是抓取响应式依赖的“触发源”与调度器“微任务队列”。通过 DevTools Performance 录制或代理 React/Vue 的更新触发函数，找出是哪个 key 在频繁触发 trigger，以及调度器为什么没能把它们合并成单次批量更新。',
    detail: `1. **排查响应式触发源 (Trigger Source)**：
   - **Vue 3**：利用 \`onRenderTriggered\` 生命周期钩子，或在组件 render 阶段打印 \`debugger\`。当依赖变更触发更新时，可以捕获到 \`event.target\`（触发更新的 reactive 对象）和 \`event.key\`，直观定位到具体是哪个属性的修改引发了 Re-render。
   - **React**：使用 React DevTools Profiler 录制更新，启用 "Record why each component rendered during hook changes"。或者利用自定义 hook 监听组件的 Props 变化，对比前后引用是否发生改变。
2. **审查调度器队列与批量更新 (Scheduler & Batching)**：
   - Vue 3 内部使用微任务队列 (\`queueJob\`) 来缓存并去重同一个 tick 里的副作用更新。如果发生频繁重渲染，通常是因为在不同的微任务/宏任务 tick 中同步触发了多次修改，导致调度器无法在同一个 tick 合并更新。
   - 排查代码中是否存在大量的异步操作（如连续 \`await\` 后的同步状态赋值），或者频繁交替触发 DOM 读写导致的同步布局。
3. **大厂防拷打延伸**：
   - **追问**：如果在 Vue 中连续修改 100 次 reactive 状态，组件会重渲染 100 次吗？
   - **解答**：不会。Vue 的 \`effect\` 执行器在触发 \`trigger\` 时，会将更新任务推入全局的 \`queue\` 队列，并通过 \`Promise.resolve().then()\` 在微任务中异步刷新队列。只要这 100 次修改是同步发生的，调度器在微任务执行前就已经把重复的 job 去重了，最终只会重渲染 1 次。`,
    docLink: { moduleId: 'vue-special', docIdx: 1, name: '阅读关联文章：Vue effect 渲染与调度' }
  },
  'Diff 为什么需要从后向前插入，锚点选择会影响什么？': {
    brief: 'Diff 从后向前插入是为了利用浏览器的 insertBefore API，该 API 要求必须已知“后置参考节点（锚点）”。锚点选择不当会导致 DOM 节点插入顺序错乱，甚至导致所有子节点发生全量重新挂载的性能灾难。',
    detail: `1. **为什么从后向前插入**：
   - 原生 DOM API \`Node.insertBefore(newNode, referenceNode)\` 是向指定参考节点之前插入新节点。如果参考节点（锚点）为 null，则默认插入到父节点的末尾。
   - 在进行 Diff 算法（如双端 Diff 或快速 Diff）的移动与新增阶段时，从后向前遍历可以保证每一次被移动/插入的节点，其后面的邻近节点已经是被 Diff 完毕、处于正确最终位置的稳定节点。因此可以直接以该稳定节点作为 \`referenceNode\` 进行插入。
2. **锚点选择的影响**：
   - 锚点（Anchor）决定了移动和挂载的基准。如果在双端或快速 Diff 的移动步骤中，选择了一个未被处理、位置尚未固定的动态节点作为锚点，一旦该锚点随后被移动，之前插入的节点也会跟着发生位置错乱。
   - 在挂载新节点时，必须动态计算出其在旧子节点序列中右侧第一个未被删除且位置已正确的节点。如果找不到，则以 null（即父容器末尾）作为锚点。
3. **大厂防拷打延伸**：
   - **追问**：React 的 Diff 算法和 Vue 的双端/快速 Diff 在移动节点上有何不同？
   - **解答**：React 采用的是单向从左向右的 Diff，它维护一个 \`lastPlacedIndex\`（最大遇到索引）。React 的移动规则是，如果当前节点在旧集合中的索引小于 \`lastPlacedIndex\`，就需要移动它。相比 Vue 的双端 Diff，React 在某些逆序场景下（例如将最后一个节点移到最前面）会产生更多的 DOM 移动操作，性能开销相对更大。`,
    docLink: { moduleId: 'vue-special', docIdx: 2, name: '阅读关联文章：Vue 核心 Diff 算法图解' }
  },
  '组件库里什么时候适合 provide/inject，什么时候不适合？': {
    brief: 'provide/inject 适用于“强耦合、嵌套层次深、且仅在局部作用域共享状态”的父子组件群（如 Form/FormItem、Select/Option）；不适合在跨模块、大范围松散业务组件中替代全局状态管理，因为这会导致依赖关系隐式化、类型难以追踪、且会造成大范围无效重渲染。',
    detail: `1. **推荐使用的场景 (高内聚子组件群)**：
   - **嵌套层级未知且深**：当组件存在父子容器嵌套（如 \`Menu -> SubMenu -> MenuItem\`），如果使用 props 逐层透传（Props Drilling），会造成中间组件极其臃肿。通过 \`provide/inject\` 可以跨级实现状态隐式共享。
   - **逻辑强耦合的局部上下文**：\`Form\` 需要收集所有子 \`FormItem\` 的校验规则与数据。在 \`Form\` 中 \`provide\` 表单实例，在 \`FormItem\` 中 \`inject\` 并注册自身，能够非常优雅地解耦具体的层级布局。
2. **不推荐使用的场景 (松散组件/业务跨层级)**：
   - **隐式耦合难以追踪**：过度使用会让子组件严重依赖外部上下文。一旦脱离特定的父组件单独使用，子组件就会因找不到 inject 的变量而报错。
   - **类型推导与契约脆弱**：在 TypeScript 难以强约束 \`provide\` 数据结构的情况下，容易导致运行时 undefined 异常。
   - **响应式污染与性能风险**：如果 provide 的是一个深层嵌套大对象，且子组件未作优化，一旦对象内属性改变，所有 inject 该对象的后代组件都会被迫触发更新，哪怕它们只用了对象中未改变的属性。
3. **大厂防拷打延伸**：
   - **追问**：如何在 Vue 3 中防止 inject 的数据被子组件任意修改？
   - **解答**：推荐在 provide 响应式数据时，配合 \`readonly\` 包裹，只允许子组件只读：\`provide('key', readonly(state))\`。如果子组件确实需要修改状态，应当在 provide 中显式传递一个改变状态的 factory 函数（如 \`updateState\`），让状态的修改权收拢在 provide 端，实现单向数据流。`,
    docLink: { moduleId: 'vue-special', docIdx: 4, name: '阅读关联文章：Vue provide / inject 原理' }
  },
  'React 里 urgent update 和 transition update 的区别是什么？': {
    brief: 'urgent update（紧急更新）是与用户物理交互（打字、点击）直接相关的任务，React 会以最高优先级同步/即时渲染以防掉帧；而 transition update（过渡更新）代表非紧急的视图转换（如列表过滤、页面切换），React 会在后台以低优先级进行“并发渲染”，允许其中途被新的紧急输入中断，从而保证页面不会卡顿。',
    detail: `1. **Urgent Update (紧急更新)**：
   - 默认的更新类型，如直接调用 \`setState\`。
   - 核心任务：直接响应物理交互。例如用户在 input 框中打字，必须立刻把字符渲染出来。
   - 调度策略：React 调度器分配最高优先级，必须在最短时间内占用主线程完成渲染，无法被中断。
2. **Transition Update (过渡更新)**：
   - 通过 \`startTransition(() => setState(...))\` 或 \`useTransition\` 包裹的更新。
   - 核心任务：处理大渲染量的非交互性计算。例如根据打字的内容过滤包含 5000 条数据的列表。
   - 调度策略：属于低优先级更新。React 开启并发模式，把渲染任务拆分为多个时间片（Time Slicing）。如果在后台渲染过渡状态的中途，用户又敲击了键盘，React 会立刻“中断”当前的过渡渲染，优先响应键盘输入（Urgent），等键盘输入渲染完毕后，再基于最新的输入值在后台重新开始过渡渲染。
3. **大厂防拷打延伸**：
   - **追问**：useTransition 和防抖节流（Debounce/Throttle）的根本区别是什么？
   - **解答**：防抖和节流是“推迟”了 JS 任务的触发时机，但一旦触发，该 JS 计算和重渲染依然会“一次性、同步且不可中断”地霸占主线程，此时页面依然会卡住。而 \`useTransition\` 并不推迟任务的触发，它是在**“渲染执行阶段”**把大任务切片，并允许用户最新的操作随时**“打断并废弃”**旧的渲染，是从浏览器底层渲染调度层面的极致优化。`,
    docLink: { moduleId: 'react-special', docIdx: 0, name: '阅读关联文章：React 并发渲染与调度机制' }
  },

  // 2. 工程化与交付稳定性
  'CI 构建偶发失败，你会先查业务代码还是依赖链路？为什么？': {
    brief: '面对 CI 偶发失败，第一步绝对是先排查“依赖链路与构建环境”。因为业务代码如果是确定性的，要么每次都失败，要么都成功；而偶发性失败通常是由锁文件失效、网络抖动、未锁定的间接依赖（Transitive Dependency）更新或 CI 缓存失效导致的“环境不一致”引起的。',
    detail: `1. **排查策略与决策树**：
   - **排查依赖链路（优先级最高）**：首先查看 \`package-lock.json\` 或 \`yarn.lock\` 是否在近期 MR 中被误改。接着排查是否存在“幽灵依赖”或者某些三方包在 \`package.json\` 里写了 \`^\` 范围，但其在 CI 干净构建时下载了带有 Bug 的最新小版本（间接依赖未被锁定）。
   - **排查构建环境**：检查 CI 容器的磁盘空间是否存满、NPM 私有源网络是否偶发性超时（连接超时导致三方包下载不全）、多线程并行构建时是否存在内存溢出（OOM）。
   - **排查业务代码中的偶发性隐患**：如果环境无误，再排查代码。重点在于：异步动态 import 顺序冲突、组件库依赖的 PeerDependencies 存在多版本共存冲突、或者测试用例中含有依赖真实时间的逻辑导致时区或边界数值偶发失败。
2. **大厂防拷打延伸**：
   - **追问**：如何彻底杜绝因间接依赖更新导致的 CI 偶发失败？
   - **解答**：必须严格执行依赖锁控制。① 在 CI 构建命令中，弃用 \`npm install\`，改用 \`npm ci\`（或 \`pnpm i --frozen-lockfile\`），强制要求必须完全按照 lockfile 的记录进行依赖安装，如果 lockfile 与 package.json 不一致则直接报错；② 引入依赖安全审计，对于没有 lockfile 的内部二方库，在打包发布产物时将其依赖打平（BundledDependencies）或在构建 CI 容器时将 node_modules 固化进基础镜像中。`,
    docLink: { moduleId: 'engineering-toolchain', docIdx: 0, name: '阅读关联文章：前端依赖树深度解析' }
  },
  'Vite 本地正常、生产白屏，你的排查顺序是什么？': {
    brief: 'Vite 生产白屏的根源在于本地开发（ESM 无需打包）与生产构建（Rollup 打包编译）的双引擎差异。排查顺序为：首先控制台定位报错位置（语法错误 vs 运行时错误），接着排查浏览器 ES6+ 语法兼容性，然后定位外部依赖 CommonJS 混入打包引发的 polyfill 缺失与 CSS 引入路径问题。',
    detail: `1. **标准排查时序流**：
   - **第一步：看控制台（Console）报错**：
     - 若报 \`Uncaught SyntaxError: ...\`：通常是打包产物包含了现代语法（如可选链、空值合并），而部署运行的宿主浏览器（如低端移动端端侧、旧版 WebView）不支持，需要调整 build.target 或引入 legacy 插件。
     - 若报 \`ReferenceError: global is not defined\` 或 \`require is not defined\`：代表某个三方 npm 依赖是旧的 CommonJS 格式，打包时 Rollup 的 CJS 转 ESM 转换失败，导致浏览器环境下无法识别 Node 独有变量。
   - **第二步：排查编译靶向与兼容性**：
     - 检查 \`vite.config.js\` 中的 \`build.target\`，是否对低版本浏览器缺失 Polyfill。
     - 检查是否混用了不支持 ESM 的第三方依赖，考虑使用 \`@vitejs/plugin-legacy\` 进行双产物兜底。
   - **第三步：排查路径与路由问题**：
     - 检查静态资源 base 根路径配置是否正确，尤其是在非根目录下部署时，避免资源 404 导致白屏。
     - 检查单页应用路由是否开启了 History 模式，但服务端没有配置 URL 重写（Nginx fallback to index.html）。
2. **大厂防拷打延伸**：
   - **追问**：为什么 Vite 本地开发很快，生产打包却经常遇到依赖报错？
   - **解答**：Vite 在本地开发时利用浏览器原生 ESM 特性，通过 esbuild 仅对依赖进行预构建（Pre-bundling）然后按需加载；而在生产环境为了极致的包体积和网络传输性能，改用 Rollup 进行 Tree-shaking 深度打包和代码分割。如果某个依赖的代码中存在不标准的 ESM 语法或动态 CommonJS 导入，本地 esbuild 能够容错，但 Rollup 严格编译时就会报错或产生错误的混淆代码，导致线上运行时白屏。`,
    docLink: { moduleId: 'engineering-toolchain', docIdx: 2, name: '阅读关联文章：Vite 核心原理与双引擎设计' }
  },
  '组件库升级拖垮业务时，产物和 peerDependencies 怎么治理？': {
    brief: '组件库升级拖垮业务，主要原因是“依赖版本分裂”与“产物打包污染”。治理策略是严格规范 peerDependencies 约束，避免多版本库同时打包，并在组件库层输出无污染的 ESM 纯净产物（保留 Tree-shaking 路径），配合 CSS 样式作用域隔离，避免业务层样式与 JS 被大量冗余打包。',
    detail: `1. **peerDependencies 严格治理**：
   - 明确组件库与底层运行时框架（如 React/Vue）的“共存关系”。在组件库的 \`package.json\` 中，必须将 \`react\` 或 \`vue\` 声明在 \`peerDependencies\` 中，而不是 \`dependencies\`。
   - 这样可以强制要求宿主业务项目来决定这些共用依赖的具体版本，避免在打包时，业务项目里既打包了业务的 \`React 18\`，又打包了组件库里私有的 \`React 17\`，造成双版本分裂白屏和包体积暴增。
2. **组件库产物优化 (CSS & JS 分流)**：
   - JS 产物必须提供 **ESM (ES Modules)** 格式，并设置 \`sideEffects: false\`（除样式文件外），让业务打包工具（Webpack/Vite）能够实现高精度的 Tree-shaking，只打包业务用到的 2-3 个组件，而不是把整个库打包进来。
   - 样式文件治理：避免组件库将 CSS 直接打包进 JS（会导致重复内联样式，无法被外部覆盖且撑大体积）。推荐将样式抽离为独立的 CSS 文件，由业务端按需引入，或使用 CSS Modules、CSS in JS 隔离作用域，防止升级带来的全局样式污染。
3. **大厂防拷打延伸**：
   - **追问**：在 Monorepo 架构中，如何预防多个子项目对同一个组件库依赖版本不一致导致的构建冲突？
   - **解答**：可以使用 pnpm 的 pnpm-workspace.yaml 和全局锁机制。在 monorepo 根目录下锁定组件库的单一版本，通过 \`pnpm.overrides\` 强制将所有子包中的多版本组件库引用重定向到指定版本，并在 CI 中运行 \`pnpm dedupe\` 自动清理重复包，从而在物理上消灭多版本共存的隐患。`,
    docLink: { moduleId: 'engineering-toolchain', docIdx: 3, name: '阅读关联文章：组件库产物与工程规范' }
  },

  // 3. 性能诊断与线上定位
  '客户反馈首屏慢，你怎么证明慢在哪里？': {
    brief: '证明首屏慢在哪里，核心在于『指标拆解与链路归因』。不能仅凭开发机单次测试，必须依赖真实用户监控（RUM）的 P75 LCP 指标，将其拆解为：TTFB（网络与服务端）、资源加载延迟（发现时机）、资源加载耗时（传输效率）和元素渲染延迟（主线程与渲染阻塞）四个子阶段。通过定位这四个阶段的耗时占比，才能科学证明瓶颈发生在网络、服务端、资源打包还是客户端 hydration 阶段。',
    detail: `1. **第一步：获取首屏 LCP 元素与阶段耗时**：
   - 在线上通过配置 \`PerformanceObserver\` 监听 \`largest-contentful-paint\` 条目，或提取 RUM 平台的 LCP 详细子阶段数据：
     $$\\text{LCP} = \\text{TTFB} + \\text{Resource Load Delay} + \\text{Resource Load Duration} + \\text{Element Render Delay}$$
2. **第二步：分段归因分析**：
   - **TTFB 过长（>800ms）**：瓶颈在服务端或网关。检查 SSR 接口串行、CDN 边缘缓存未命中或物理网络延迟（可通过 DNS、TCP、SSL 阶段耗时交叉验证）。
   - **资源发现延迟过长（Resource Load Delay >10%）**：说明 LCP 元素发现过晚。通常因为图片是用 JS 动态加载的，或者被 CSS \`background-image\` 隐式引入。证明方式：检查该图片请求在 Network 面板中与 HTML 的时间差，优化方案是采用 \`<link rel="preload">\` 或 \`<img fetchpriority="high">\`。
   - **资源加载时间过长（Resource Load Duration >40%）**：网络传输慢。检查图片体积（是否采用 WebP/AVIF）、CDN 节点丢包率和带宽限制。
   - **元素渲染延迟过长（Element Render Delay >20%）**：资源已下载，但没画出来。检查主线程是否被庞大的 JS 编译执行、React Hydration 任务或强制同步布局（Reflow）抢占。
3. **第三步：灰度对比与网络隔离**：
   - 用 RUM 数据按网络（Wi-Fi/4G/3G）、设备等级（CPU 核心数、内存大小）进行切分，证明是在低端设备/弱网环境下慢，还是在全网范围内均慢。`,
    docLink: { moduleId: 'performance-diagnostics', docIdx: 1, name: '阅读关联文章：LCP 首屏链路优化' }
  },
  '用户点击后 1 秒才响应，你怎么从 INP 和 Long Task 定位？': {
    brief: '用户点击响应长达 1 秒，代表 INP（交互到下次绘制）严重超标（合格线为 200ms）。一次交互的延迟由三部分组成：输入延迟（Input Delay）、处理耗时（Processing Duration）和呈现延迟（Presentation Delay）。我会利用 Chrome DevTools 或者是线上的 LoAF（Long Animation Frame）API 定位到具体是主线程的哪个 Long Task 阻塞了输入，是哪个事件回调的 JS 代码执行太长，还是随后的 React Re-render 导致了全局样式重算与重排。',
    detail: `1. **定位输入延迟（Input Delay）**：
   - 用户点击时，主线程若有正在执行的 Task，交互只能排队。如果输入延迟长达数百毫秒，说明有其他 **Long Task（>50ms）** 在持续抢占主线程（例如：页面初始化时的其他非紧急 JS 初始化脚本）。我们可以通过 \`PerformanceObserver\` 捕获 \`longtask\`，或升级到 \`long-animation-frame\`（LoAF）抓取当前交互发生时的并发阻塞脚本来源。
2. **定位处理耗时（Processing Duration）**：
   - 这是用户点击的回调函数（JS）本身的执行时间。
   - **排查方法**：在 DevTools Performance 面板中录制用户交互，展开 Main 线程，查看该 Click 事件下方的 Flame Chart（火焰图）。如果某个自定义函数执行超过了 50ms，会显示红色警告。
   - **优化方法**：对海量数据过滤等纯 CPU 密集型计算，移入 **Web Worker**；对大型任务，使用 \`scheduler.yield()\` 或 \`setTimeout\` 进行时间切片，及时向浏览器让出主线程。
3. **定位呈现延迟（Presentation Delay）**：
   - JS 已经执行完，但浏览器没画出新像素。通常由于状态更新触发了大规模的 DOM 树重新渲染、复杂的样式计算或“读写交替”导致的**强制同步布局（Layout Thrashing）**。
   - **排查方法**：观察 Performance 中的 \`Recalculate Styles\` 和 \`Layout\` 块耗时，如果它们紧跟在事件回调后面且耗时长达几百毫秒，则说明发生了布局抖动。`,
    docLink: { moduleId: 'performance-diagnostics', docIdx: 2, name: '阅读关联文章：INP 与交互响应式优化' }
  },
  '性能优化上线后如何避免一周后又退化？': {
    brief: '防止性能退化，必须将性能指标从『单次整治』升级为『自动化工程防线』。我们需要在三个核心节点建立卡卡拦截门禁：在 CI 阶段限制构建包体积预算；在灰度发布阶段利用 RUM 数据对比新旧版本指标，实施熔断阻断；在运行期建立分钟级看板与核心指标退化告警。用自动化的工具代替人肉巡检，让性能恶化在发布前或灰度期被拦截。',
    detail: `1. **左移至 CI 门禁（编译期卡点）**：
   - 引入 **Lighthouse CI** 并在 GitHub Action / GitLab CI 中跑受控环境测试。
   - 设置包体积预算（Performance Budget），例如限制入口 Chunk 压缩后不得超过 200KB，一旦某次 MR/PR 超过预算，CI 自动标红并禁止 Merge。
2. **灰度发布熔断机制（发布期卡点）**：
   - 发布系统支持分批灰度（如 1% -> 5% -> 10%）。
   - RUM 监控中心实时按 \`release_version\` 维度计算 LCP、INP、CLS 的 P75 值，并与生产稳定版进行对比。
   - 若新版本指标发生统计学显著劣化（如 INP 慢了超过 15% 或崩溃率上升），灰度发布平台触发 **Circuit Breaker（熔断）**，自动终止后续放量并立即将灰度流量回滚到上个版本。
3. **常态化监控与报警（运行期看盘）**：
   - 设置常态化性能监控，配置告警通知（邮件/钉钉/企业微信）。
   - 定期进行性能审计和事故复盘，将性能劣化等同于线上 Bug 进行归因排查，确保优化红利不因业务快速迭代而流失。`,
    docLink: { moduleId: 'performance-diagnostics', docIdx: 4, name: '阅读关联文章：性能预算与防退化治理' }
  },

  // 4. 前端架构与复杂业务建模
  '为什么复杂表单不能只靠 useState 或一个大对象维护？': {
    brief: '复杂表单（如带动态校验、条件联动、且节点数 >100 的表单）若用 useState 或单一根对象管理，每次按键都会导致整个表单页面“全局重渲染”，引发严重输入卡顿。合理的方案是采用“订阅制表单管理器”（如 Formik / React Hook Form / AntD Form 原理），让状态更新局限在具体的 FormField 组件内部。',
    detail: `1. **性能维度的痛点 (全局重渲染阻碍输入流畅度)**：
   - 如果整个表单的状态由父组件的一个 \`useState(formData)\` 驱动，用户在某一个输入框敲击键盘修改一个字符时，由于父组件的 state 改变，所有包含在该表单内的子字段组件（甚至包括无关的静态文案、图片）都会经历完整的 Virtual DOM Diff。
   - 当表单项数量较多时，这一大片组件树的重渲染耗时很容易突破 50ms 门槛（引发 Long Task），导致用户打字时出现明显的滞后卡顿感（INP 变差）。
2. **逻辑维度的痛点 (联动与校验灾难)**：
   - 复杂表单往往伴随着错综复杂的联动关系（如选择 A 字段，B 字段隐藏，C 字段异步校验）。如果将所有联动计算都写在父组件的 render 函数里，会造成业务逻辑耦合混乱，极难维护。
3. **架构解耦方案（局部状态订阅）**：
   - 引入非响应式的 Store（通过 Ref 挂载表单值）进行表单数据的物理管理。
   - 子组件（如 \`FormField\`）以发布订阅模式订阅 Store 中与自己强相关的那个具体 path 的数据变化。
   - 当用户在特定 Field 中输入时，Store 的数据发生同步修改，但只单独触发该 Field 组件进行重新渲染，而父组件和其他 Field 组件完全保持静默。
4. **大厂防拷打延伸**：
   - **追问**：如何设计一个支持条件联动且不会导致全局重渲染的表单系统？
   - **解答**：可以设计一个 Reactive 拓扑联动计算树。使用 Form Store 维护一份依赖关系图，当 A 字段值变化时，Store 内部通过事件总线精准通知 A 依赖的联动下游组件（如 B）重新计算其 display/disabled 属性并重新渲染，这同样通过订阅分流完成，保证了高频输入时 DOM 更新路径的最短化。`,
    docLink: { moduleId: 'state-data-longform', docIdx: 0, name: '阅读关联文章：复杂表单架构与性能设计' }
  },
  '微前端带来的隔离、共享、发布和故障降级怎么权衡？': {
    brief: '微前端架构是典型的“空间换时间/自治换复杂度”的工程取舍。必须通过 JS Sandbox 与 CSS 作用域建立严格隔离，通过基座提供公共依赖的按需共享以控体积，支持子应用独立自治发布，并在基座层构建高可用的沙箱兜底与故障路由降级机制。',
    detail: `1. **隔离隔离再隔离 (Isolation)**：
   - **JS 隔离**：现代浏览器推荐使用原生 \`ShadowRealm\`，但在当前兼容性限制下，通常采用 \`Proxy\` 沙箱，代理 \`window\` 上的读写操作，在子应用卸载时还原全局环境。
   - **CSS 隔离**：使用 Shadow DOM 可以实现真正的物理隔离，但对弹窗、Portal 组件非常不友好。工程上常用 CSS Modules 或 PostCSS 自动添加子应用前缀名进行命名空间隔离。
2. **公共依赖共享 (Shared Dependencies)**：
   - 严防大体积依赖被多份打包（如 React/Vue）。
   - 解决方案：基座应用通过 **Webpack Module Federation（模块联邦）** 或 ESM Import Maps 在运行时将 React、Lodash 等库注册为 Shared，子应用启动时自动检测基座是否已挂载，若存在则复用，否则才降级自己下载。
3. **故障降级与容灾 (Graceful Degradation)**：
   - 子应用不能拖垮基座。
   - 实现方案：基座渲染子应用容器时，包裹 \`ErrorBoundary\` 组件。一旦子应用加载失败（如静态资源 404）或运行时发生未捕获崩溃，基座拦截错误，并将子应用容器优雅降级为静态骨架图，同时上报监控日志，允许用户重新发起热加载，不影响其他侧边栏或主应用的运行。
4. **大厂防拷打延伸**：
   - **追问**：模块联邦和 CDN 外链 externals 共享依赖有什么区别？
   - **解答**：Externals 需要在 HTML 中手动写入硬编码的 CDN 地址，版本锁定死板，一旦升级容易引发全局污染和冲突。而模块联邦支持运行时的**“版本协商（Version Negotiation）”**，子应用和基座在各自打包时各自声明对依赖的语义化版本范围（如 \`^18.0.0\`），运行时根据加载顺序动态匹配共享最合适的高版本，更加智能和自动化。`,
    docLink: { moduleId: 'frontend-architecture-longform', docIdx: 2, name: '阅读关联文章：微前端沙箱与容灾设计' }
  },
  '组件库 API 变更如何保证业务升级成本可控？': {
    brief: '保证组件库升级成本可控，主要依靠“渐进式废弃”与“自动化升级工具（Codemod）”。禁止直接进行破坏性变更（Breaking Change），对于老旧 API 引入 \`console.warn\` 提示和 \`@deprecated\` 语法修饰，最终提供 AST 转换脚本帮助业务方一键迁移代码。',
    detail: `1. **渐进式 API 升级策略 (Deprecated -> Removed)**：
   - 第一步：在小版本中引入新属性，保持旧属性兼容。对使用旧属性的逻辑，控制台输出带有当前组件栈追踪的警告：\`[Component] Prop 'xxx' is deprecated, please use 'yyy' instead.\`，但功能不受损。
   - 第二步：在 IDE 层面使用 TypeScript 的 \`/** @deprecated */\` 注释标记旧 API。当业务研发调用该属性时，编辑器中会自动出现删除线提示。
   - 第三步：在后续大版本（Major Version）中才物理移除该 API。
2. **编写 AST 工具一键迁移 (Codemod)**：
   - 对于涉及大面积语法替换的变更（如组件名、Props 重命名），组件库团队应该提供配套的 codemod 命令行工具（基于 \`jscodeshift\` 或 \`ts-morph\`）。
   - 业务方只需在根目录下执行 \`npx my-ui-codemod --path src\`，工具便能自动将老 API 代码转换为符合新版规范的代码，减少人工查找替换的低效与遗漏风险。
3. **大厂防拷打延伸**：
   - **追问**：如何在大厂复杂的多业务树环境中验证组件库升级对下游项目的负面影响？
   - **解答**：可以建立**“金丝雀预览与依赖扫描门禁”**。在组件库每次提交 MR 时，CI 系统拉取内部有代表性的 2-3 个核心业务项目的代码镜像，在沙盒中利用新版 UI 执行编译和单测测试（甚至自动化 UI Diff 测试），检测是否会导致业务项目编译挂掉。这种将“业务项目当做组件库端到端测试用例”的逆向校验方案，能极大地提升大版本发布的稳定性。`,
    docLink: { moduleId: 'frontend-architecture-longform', docIdx: 3, name: '阅读关联文章：组件库 API 升级稳定性设计' }
  },

  // 5. 质量、安全与可维护性
  '一个支付链路前端改动，你会怎么设计测试分层？': {
    brief: '针对支付这类的黄金转化链路，必须实施金字塔式严密防御：底层以 Unit Test 确保金额计算等纯逻辑的精确性；中层以 Component Integration Test 模拟表单联动与协议确认；顶层引入 E2E 视觉回归与 Mock 真实网络链路测试，并在灰度期引入监控指标进行最后一公里验证。',
    detail: `1. **测试金字塔架构 (Pyramid of Testing)**：
   - **单元测试 (Unit Test - 占比 60%)**：针对核心的底层工具函数（例如金额格式化、数字高精度相加算法、支付状态映射码）。使用 Vitest/Jest 进行高密度的边界值边界条件测试（如 0 元、负数、极大数、空状态等），确保这些没有 DOM 的纯 JS 逻辑 100% 正确。
   - **组件/集成测试 (Integration Test - 占比 30%)**：针对收银台组件、优惠券选择列表等交互逻辑。通过 Testing Library 模拟用户点击优惠券、确认复选框协议、按钮置灰与激活态的状态流转，确保表单组件和外部状态管理库的交互符合契约。
   - **端到端测试 (E2E Test - 占比 10%)**：针对完整的支付流程（从选商品 -> 提交订单 -> 进入收银台 -> 点击支付 -> 跳转成功页）。使用 Playwright 或 Cypress 编写核心 Happy Path 的自动化脚本。为了测试稳定性，这部分可以针对预发环境并 Mock 支付网关返回（但必须全流程运行在真实浏览器环境中）。
2. **运行期防护与监控**：
   - 在前端支付按钮的点击、API 返回阶段埋设详细的埋点（Click、Submit、Response_Success、Response_Fail）。
   - 配置实时告警：一旦某次发布后，线上支付按钮的点击到成功转化的漏斗发生统计学显著下跌，或者支付接口报错率突增，自动电话/短信报警，立即触发发布平台的一键自动回滚。`,
    docLink: { moduleId: 'testing-quality-longform', docIdx: 0, name: '阅读关联文章：测试分层与可观测性实践' }
  },
  '富文本输入如何避免 XSS，同时不影响业务表达？': {
    brief: '避免富文本 XSS 的黄金法则是“过滤输入、净化渲染、严防 DOM 逃逸”。不能盲目把所有的 HTML 字符串转义（会破坏样式排版），必须使用成熟且维持更新的净化库（如 DOMPurify）按白名单策略过滤掉危险的 script、onload 等节点与属性，并利用 Trusted Types 建立浏览器级别的防护网。',
    detail: `1. **基于白名单过滤 (HTML Sanitization)**：
   - **输入端**：在富文本编辑器（如 Quill、Slate）生成 HTML 产物提交接口前进行初步过滤，但不能以此为唯一防线（因为攻击者可以通过 curl 绕过前端直接发包）。
   - **渲染端（重中之重）**：前端从 API 拿到富文本字符串在 React/Vue 中渲染（如 React 的 \`dangerouslySetInnerHTML\` 或 Vue 的 \`v-html\`）时，必须先通过 \`DOMPurify.sanitize(dirtyHtml)\` 过滤。
   - DOMPurify 内部构建了严密的 DOM 解析白名单。它允许 \`<p>\`, \`<div>\`, \`<img>\`, \`<a>\` 等常见排版标签，并且只保留 \`href\` (需排除 javascript: 协议)、\`src\` 等安全属性，会将 \`<script>\`、\`<iframe>\`、\`onerror\`、\`onload\` 等恶意执行脚本物理过滤掉。
2. **启用 CSP 与浏览器原生 Trusted Types**：
   - 配置 Content Security Policy (CSP)，限制脚本只能从可信域名加载，禁止执行 inline script。
   - 在支持的现代浏览器中开启 **Trusted Types（可信类型）**。一旦开启，如果直接将纯字符串赋给 \`innerHTML\`，浏览器控制台会拒绝渲染并报错；你必须通过安全策略工厂（DOMPurify 包装）生成一个经过审计的可信对象，浏览器才允许其插入 DOM，这在引擎层彻底封死了 DOM-based XSS 的逃逸通道。
3. **大厂防拷打延伸**：
   - **追问**：如何防止 a 标签的 href="javascript:alert(1)" 绕过 XSS 检查？
   - **解答**：这属于典型的协议头绕过攻击。防范手段有二：① 在 DOMPurify 中显式配置 ALLOWED_URI_REGEXP，只允许 http、https、mailto 等安全协议头，拒绝任何以 javascript: 开头的地址；② 在渲染链接时，在全局拦截 click 事件，或者统一加上 \`rel="noopener noreferrer"\` 属性，并在业务层增加 URL 安全检测。`,
    docLink: { moduleId: 'security-content', docIdx: 0, name: '阅读关联文章：XSS 深度攻防与安全实践' }
  },
  '依赖包被污染时，前端构建链路有哪些防线？': {
    brief: '当第三方 npm 依赖包被黑客投毒（如 node-ipc 投毒事件），前端构建链路有三层铁卡防线：第一层在本地与 CI 安装时锁定哈希值校验；第二层在发布门禁中进行依赖漏洞和供应链成分分析（SCA）静态扫描；第三层利用构建沙盒限制 node 进程访问敏感文件及网络。',
    detail: `1. **第一防线：依赖哈希强锁定 (Lockfile Integrity)**：
   - 必须将 \`package-lock.json\` 提交到 Git。Lockfile 中记录了每个依赖包在 npm 官方下载时的 **integrity (哈希校验值，如 sha512)**。
   - 在 CI 阶段，执行 \`npm ci\` 或 \`pnpm install --frozen-lockfile\`。如果黑客直接在 npm 仓库中对同一个版本（如 1.2.3）的包包体进行物理篡改和投毒，其包文件的 md5/sha512 哈希值就会与 lockfile 中存储的值不匹配，NPM 包管理器会立刻拒绝安装并熔断构建。
2. **第二防线：静态安全审计与扫描 (SCA / Snyk / npm audit)**：
   - 在 CI 构建流水线中，增加 \`pnpm audit\` 或集成专业安全扫描工具（如 Snyk、Dependabot、SonarQube）。
   - 对依赖进行扫描，一旦发现当前依赖树中包含了已被公开记录的 CVE 漏洞或恶意后门软件包，直接阻断构建。
3. **第三防线：构建运行沙箱隔离 (Host Isolation)**：
   - 很多黑客投毒包是在安装阶段（如 \`postinstall\` 脚本）或者构建编译阶段在 node 执行环境下读取敏感文件。
   - 必须在 CI 中配置 Docker 沙盒，限制构建容器只能访问当前工作目录，彻底阻断其访问宿主机的 \`/etc/passwd\`、\`.ssh\` 秘钥文件；同时关闭构建沙盒的广域网访问权限（除非是拉包阶段），防止黑客脚本把窃取的私钥通过 HTTP 发送到黑客服务器。
4. **大厂防拷打延伸**：
   - **追问**：如何在本地日常开发中预防黑客投毒包修改本地代码？
   - **解答**：可以配置 \`package.json\` 的全局设置，禁用第三方包的脚本自动执行，例如通过 \`pnpm install --ignore-scripts\`。这会阻止依赖包在安装时执行任何编译/侵入式脚本，从源头上消灭了安装阶段投毒的可能。`,
    docLink: { moduleId: 'security-content', docIdx: 2, name: '阅读关联文章：供应链安全与依赖加固' }
  },

  // 6. 项目表达与面试追问
  '这个项目最难的问题是什么，你为什么认为它难？': {
    brief: '描述项目“最难问题”的黄金法则是：遵循“背景 - 难点 - 方案取舍 - 数据佐证”的 STAR 原则，且难点必须是“深层次、跨系统的技术权衡”，而不是因为自己不会写某段代码。要体现你在高并发、大数据量、复杂状态管理或多团队协同中的架构思考。',
    detail: `1. **STAR 面试表达框架**：
   - **背景 (Situation)**：一句话说明项目做什么（如：为了解决高频交易系统数据展示延迟的问题）。
   - **难点 (Task - 最关键)**：解释“为什么它难”。难点应该是一个两难的**“架构权衡”**。比如：“难在数据量级极大（同时渲染 2 万条动态数据且带实时推送），但主线程算力非常有限。如果全量渲染会导致页面 100% 卡死（INP 飙升到 2s）；如果采用简单分页，又无法满足交易员全局监控的硬性业务需求。”
   - **方案与取舍 (Action)**：对比 2-3 种行业通用做法，说明你的技术选型取舍。比如：“对比了 WebGL Canvas 渲染和虚拟列表。虽然 WebGL 性能极佳，但事件绑定复杂，不利于快速迭代。最终决定自研动态高度虚拟列表，并配合 RequestIdleCallback 队列式更新 Store 状态，利用 Web Worker 进行数据流解压和归并排序。”
   - **结果 (Result)**：提供客观的数据量化指标。“重构后，P75 LCP 从 4.2s 缩短至 1.5s，在 3 万行数据滚动的极端负载下，INP 稳定保持在 120ms 内，主线程 Long Task 发生率降低了 85%，保证了交易的高实时响应。”
2. **大厂防拷打延伸**：
   - **追问**：如果你的虚拟列表在快速滑动时发生了严重的白屏闪烁，你怎么解决？
   - **解答**：这属于典型的缓冲区设置不合理以及渲染掉帧。① 引入 \`prerender\` 缓冲区，在可见视口上下方多预渲染 1.5 页的节点，提供视觉缓冲；② 开启 CSS 的 \`will-change: transform\` 提升绘制为独立合成层，避免滚动时触发全局重绘；③ 配合 \`requestAnimationFrame\` 对滚动事件监听进行合并，平滑视口计算与 DOM 替换的节奏，消除因为主线程计算落后于 GPU 滚动带来的视口真空闪烁。`,
    docLink: { moduleId: 'project-prep-special', docIdx: 1, name: '阅读关联文章：大厂面试项目重难点提炼' }
  },
  '如果流量放大十倍，你的方案哪里会先出问题？': {
    brief: '面对“流量放大十倍”的系统设计极限追问，切忌回答“没问题”。合格的前端架构师必须具备清晰的系统容量意识，能够推演出系统最薄弱的瓶颈点：通常首先出问题在“网络带宽过载与网关限流”、“接口海量串行请求卡死连接池”以及“客户端大数据量一次性加载导致主线程内存溢出”。',
    detail: `1. **推演瓶颈与连锁反应 (Bottlenecks)**：
   - **第一关：网关与接口并发限制**：
     - 当前端发起的静态页面或 API 请求并发量骤增 10 倍时，如果网关没有做好限流，服务端连接池会被瞬间占满，导致前端大面积请求报 502/504 错误。
     - 排查与防御：在前端网关层（如 Nginx/BFF）配置静态资源强缓存，对非核心接口增加熔断器，避免单点 API 被拖垮导致整个系统雪崩。
   - **第二关：客户端连接占用与拥堵**：
     - 浏览器对同一个域名的 TCP 并发连接是有 6 个的上限限制的（HTTP/1.1）。10 倍流量下，如果页面资源和接口没有合理的编排，会导致大量的请求处于 \`stalled\`（排队挂起）状态，严重推迟 LCP 时间。
     - 优化：引入 HTTP/2 协议（多路复用，消除队头阻塞）；将静态资源上传至独立的 CDN 域名，实现域名分片与接口隔离。
   - **第三关：客户端内存与渲染性能坍塌**：
     - 若服务端一次性返回的数据量大 10 倍，如果前端没有使用分页或流式加载，JS 内存（Heap）会在短时间内发生指数级膨胀，频繁触发垃圾回收（GC GC Pause），导致页面帧率突降、打字交互无响应，甚至触发低端机 OOM 闪退。
2. **大厂防拷打延伸**：
   - **追问**：在大促流量洪峰下，如果某个非核心依赖包挂了，如何保证用户的基本交易不受影响？
   - **解答**：实施**“柔性降级（Resilience Degradation）”**。① 在静态代码层，对所有三方 SDK（如推荐广告、埋点统计）使用动态 \`import()\` 异步加载，并包裹 \`try-catch\`。即使这些外部 CDN 挂了，也只影响局部挂件，不阻断主交易核心流程加载；② 在接口层，实现离线缓存或本地兜底 LocalStorage 机制，当网络不可用或返回 500 时，渲染本地缓存的静态快照数据，确保系统“带伤运行”。`,
    docLink: { moduleId: 'project-prep-special', docIdx: 2, name: '阅读关联文章：高并发场景下的前端容灾与降级' }
  },
  '你如何证明这个优化真的改善了客户问题？': {
    brief: '证明优化的价值，绝不能说“我的电脑变快了”或“Lighthouse 跑了 99 分”。必须用“业务数据 + 线上 RUM 数据”的双重数据闭环证明。要证明特定优化发布后，新版本的用户跳出率、业务转化率呈统计学正向提升，同时 P75 LCP、INP 指标在相同网络、设备基准下有真实缩短。',
    detail: `1. **数据闭环证明链 (Double Data Closed-loop)**：
   - **技术性能数据（RUM 证据）**：
     - 在大盘中提取同一路由、同一端侧（如 iOS 微信 WebView）在新旧版本（Release Version）下的 Web Vitals 对比。
     - “我们的 P75 LCP 从优化前的 3.8s 降到了优化后的 1.8s。这是基于 10 万级真实用户 PV 计算得出的，排除了开发机和本地实验室环境的幸存者偏差。”
   - **业务收益数据（商业价值证据 - 核心）**：
     - 技术优化必须为业务转化服务。比如：“根据漏斗数据监控，当我们将详情页 LCP 优化到 2 秒内后，由于页面白屏时间大幅缩短，首屏流失率（跳出率 Bounce Rate）降低了 4.2%，订单提交按钮的最终转化率（Conversion Rate）提升了 1.5%，直接拉动了业务 GMV 的增长。”
     - 这一套将“性能指标 -> 跳出率 -> 转化率 -> GMV”串联的证据链，能瞬间说服面试官和业务团队。
2. **科学验证排他法 (A/B Test)**：
   - 为了证明转化率提升真的是由“性能优化”带来的，而不是因为当周业务大促发券的自然增长，最好的证明手段是 **A/B Test 灰度测试**。
   - 对 50% 用户发布优化后的高性能版本（A 组），50% 用户维持老版本（B 组），在相同的营销活动周期内跑大盘。如果 A 组的用户转化率和体验评分明显高于 B 组，即可用统计学显著性检验（P-value < 0.05）铁证如山地证明性能优化的巨大商业价值。`,
    docLink: { moduleId: 'project-prep-special', docIdx: 3, name: '阅读关联文章：性能优化商业价值量化与数据闭环' }
  }
};
