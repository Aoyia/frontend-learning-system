import vue3ReactivityMarkdown from '../日常工作学习过程中的看过的文档/vue3响应式原理.md?raw';
import vue3DiffMarkdown from '../日常工作学习过程中的看过的文档/vue3的 diff.md?raw';

export const VUE_SPECIAL_CONTENT = {
  id: 'vue-special',
  name: 'Vue 专项模块',
  icon: '🖖',
  desc: '围绕 Vue 核心机制与面试高频考点构建的专项学习与刷题模块。',
  docs: [
    {
      title: 'Vue 3响应式原理源码解析',
      difficulty: '困难',
      sourceCards: ['Vue 3 响应式系统源码'],
      content: vue3ReactivityMarkdown,
      quiz: [
        {
          type: 'single',
          question: 'Vue 3 中 `reactive` 创建响应式对象的核心能力来自哪种机制？',
          options: ['Object.defineProperty', 'Proxy', 'Reflect.preventExtensions', 'JSON.stringify'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果不使用 Proxy 而是使用 Object.defineProperty，Vue 2 就无法检测到对象属性的新增和删除，也无法直接拦截数组的索引修改和长度变化，需要通过 Vue.set/$set 和重写数组原型方法来打补丁，造成了 API 的不一致性和额外的运行时心智负担。

🔍 核心原理解析（防拷打）：
1. Proxy 在目标对象前架设一层拦截，可以对 13 种操作（如 get, set, deleteProperty, has, ownKeys 等）进行代理，完美解决动态增删属性的拦截。
2. 性能与内存取舍：defineProperty 需要在初始化时递归遍历对象所有属性进行双向绑定，对于大对象性能开销巨大；Proxy 是惰性拦截（调用 get 时才递归代理下一层），首屏渲染 and 内存占用有极大提升。
3. 进一步拓展大厂面试追问：Proxy 只能代理对象，对原始值（number, string等）无能为力，因此 Vue 3 引入了基于 get/set 访问器的 ref；此外，Proxy 存在 target 内部 this 指向代理对象而非原对象的问题，Vue 内部通过 Reflect.get/set 并传递 receiver 保证 this 指向正确。`,
        },
        {
          type: 'multiple',
          question: '关于 Vue 3.5+ 响应式依赖模型，以下哪些描述是正确的？',
          options: [
            'Dep 触发时会维护版本号用于脏检查',
            '依赖结构可通过 Link 节点构成双向关联',
            '每次 effect 运行都必须完整删除并重建所有依赖',
            '依赖稳定时可复用已有关系以减少无效操作',
          ],
          answer: [0, 1, 3],
          explain: `💡 它解决了什么问题：
如果不想办法优化依赖追踪，在 3.4 之前，每次副作用（effect）运行都需要将上一次收集的所有依赖全部清除（cleanup），然后重新执行并重新收集。在大对象、高频触发或复杂计算属性嵌套场景下，高频的 Set/Map 读写与垃圾回收（GC）会导致明显的 CPU 抖动和内存开销。

🔍 核心原理解析（防拷打）：
1. 3.5 引入了 Link 节点构成的双向链表，使 Dep 和 Subscriber 之间形成双向关联，既能高效遍历下游，也能从下游清理无效订阅。
2. 设计取舍：废弃了 3.4 之前使用 Set 存储依赖的方案，改用 Link 链表节点。Link 节点在 effect 运行前被标记为可能过期，并在执行过程中被复用（状态置为已确认），避免了频繁创建和销毁 Set 带来的垃圾回收负担，内存开销降低了大约 50%。
3. 进一步拓展大厂面试追问：当 reactive 属性值没变时，Link 中的 version 计数能够用于跳过不必要的下游计算，防止 computed 嵌套链引发无效更新风暴。`,
        },
        {
          type: 'judgment',
          question: '`effect` 嵌套时，如果不恢复上一个 active effect，上下文可能会错乱。',
          options: ['对', '错'],
          answer: 0,
          explain: `💡 它解决了什么问题：
若不保存并恢复上一个 active effect/activeSub，在组件嵌套渲染或计算属性嵌套等场景下，子 effect 执行完后会将全局的当前 activeSub 设为 null。当父 effect 后续的响应式数据被读取时，其依赖将无法被正确收集到父 effect 中，导致响应式依赖链路断裂、视图不更新。

🔍 核心原理解析（防拷打）：
1. Vue 3 内部维护了一个全局的 activeSub 变量。当执行某个 ReactiveEffect 的 run 方法时，会先保存先前的 activeSub 到局部变量，执行完毕后在 finally 块中将其还原。
2. 设计取舍：在早期版本中，Vue 3 使用一个数组栈 effectStack 来维护嵌套关系，3.2+ 后为了优化内存和减少数组 push/pop 开销，改用对象属性（链表指针如 effect.parent 或局部变量暂存）进行上下文恢复，大大降低了垃圾回收频率。
3. 进一步拓展大厂面试追问：如果副作用中存在异步操作（如 await 后的数据读取），由于微任务执行脱离了同步的 setup/run 栈，全局 activeSub 已恢复或置空，从而导致异步读取的属性无法被收集。这是为什么 watch/computed 的 getter 必须是同步执行的核心原因。`,
        },
        {
          type: 'single',
          question: '下面哪一项最符合 `computed` 的核心特征？',
          options: ['每次依赖变更都立即重新计算并立刻执行 getter', '不缓存结果，只做透传', '惰性求值并缓存，依赖变化后标记为脏', '只能用于模板中，不能在 JS 中访问'],
          answer: 2,
          explain: `💡 它解决了什么问题：
如果不引入惰性求值和缓存机制，每次依赖的响应式数据发生变化时，计算属性都会立即同步重新执行。当模板中多次访问或多个计算属性相互嵌套时，会导致大量无意义的重复计算，严重拖慢渲染性能。

🔍 核心原理解析（防拷打）：
1. Computed 内部封装了一个 ReactiveEffect，其调度器（scheduler）在依赖变更时仅将 dirty 标志置为脏，并不执行计算；只有当外部读取 computed.value 时，才会触发脏检查并重新计算。
2. 设计取舍：Vue 放弃了立即更新方案，选择把性能开销推迟到实际被读取的时刻。在 3.5 版本中，更引入了全局版本号与依赖版本比对，在依赖的实际值没有改变时，连脏检查后的 getter 执行都能免除。
3. 进一步拓展大厂面试追问：如果 computed 的 getter 中存在副作用操作（例如修改其他响应式状态），会导致依赖收集混乱甚至无限死循环。因此 computed 应当作为纯函数使用，不应包含任何副作用。`,
        },
        {
          type: 'multiple',
          question: '以下哪些 API/机制与“副作用生命周期管理”直接相关？',
          options: ['effectScope', 'scope.stop()', 'WeakMap targetMap', 'watchEffect 返回的停止函数'],
          answer: [0, 1, 3],
          explain: `💡 它解决了什么问题：
如果无法集中管理和回收副作用，当我们在组件外部或异步操作中动态创建多个 watch、computed 或 effect 时，它们在组件销毁后仍会常驻内存、持续监听响应式变化，从而导致严重的内存泄漏和意料之外的后台业务逻辑执行。

🔍 核心原理解析（防拷打）：
1. effectScope 允许创建一个作用域，在此作用域内创建的任何响应式副作用都会被自动收集到该 scope 的 effects 数组中。调用 scope.stop() 时，会遍历并执行所有收集到的副作用的停止逻辑（如 cleanup、stop 等）。
2. 设计取舍：不需要手动维护一个巨大的清除函数数组。Vue 将副作用实例与 Scope 节点相连，利用作用域的树状层级关系，实现一键式深度回收。
3. 进一步拓展大厂面试追问：在 pinia 状态管理库和 Vue 组合式函数中，effectScope 被广泛用于在组件卸载时自动注销局部 Store 的副作用，确保宿主卸载时内存彻底释放。`,
        },
        {
          type: 'single',
          question: '在响应式触发链路中，哪一步主要用于“合并通知，避免同轮重复触发”？',
          options: ['track', 'batch/startBatch/endBatch', 'toRaw', 'readonly'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果不引入 batch（批处理）合并通知机制，在单次事件循环（如用户点击事件中）连续修改多个响应式属性时，每个属性的变更都会立即、同步地触发下游的依赖订阅者（Subscriber），这会导致组件渲染 effect 重复执行 patch，引发严重的 DOM 闪烁和极其低效的渲染性能。

🔍 核心原理解析（防拷打）：
1. batch 通过 startBatch() 和 endBatch() 包裹响应式变更。变更发生时，不立即触发 effect 运行，而是先将受影响的 subscriber 标记为脏并暂存；当 endBatch 执行时，才统一释放并执行这些订阅者的通知。
2. 设计取舍：batch 负责响应式底层的依赖通知合并，而 queueJob 则是运行时调度器为了合并组件 DOM 渲染任务。两者各司其职，底层的 batch 确保了响应式数据流的干净和低开销。
3. 进一步拓展大厂面试追问：batch 内部使用的是版本计数与批处理深度计数，可以完美支持嵌套 batch 调用，并在最外层 batch 结束时一次性放行通知，这在复杂状态流转中尤为关键。`,
        },
        {
          type: 'judgment',
          question: '`reactive` 解构后仍然天然保持响应式，不需要任何额外处理。',
          options: ['对', '错'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果不理解解构丢失响应式的问题，开发者可能会直接写出类似 const { count } = reactive({ count: 0 }) 的代码，修改 count 变量时发现视图无动于衷，从而破坏了响应式系统的心智模型，导致大量逻辑失效。

🔍 核心原理解析（防拷打）：
1. ES6 的解构赋值本质上是值复制（对于基本类型）或引用复制（对于对象类型）。对于 Proxy 拦截的 reactive 对象，解构出来的属性不再是通过 Proxy 的 get 进行读取，自然脱离了依赖收集和派发更新的轨道。
2. 设计取舍：Vue 无法重写 JavaScript 语言层面的解构行为。为了解决这一痛点，Vue 提供了 toRef 和 toRefs API，它们通过创建一个继承了响应式链接的 RefImpl 实例，包装了 getter/setter 操作，从而在保持解构语法的同时间接保留了响应式。
3. 进一步拓展大厂面试追问：在 script setup 中使用 defineProps 解构时，可以使用 toRef 或配合 Vite 插件来保证解构后的响应式，但底层依然是通过属性访问器进行中转。`,
        },
        {
          type: 'single',
          question: '当 effect 正在运行且不允许递归时，`notify` 的处理策略通常是？',
          options: ['立即递归再执行一次', '直接跳过本次触发，避免循环', '抛出致命错误', '强制进入同步队列'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果在副作用执行过程中，存在类似 data.count++（先 get 收集依赖，再 set 触发更新）的同步操作，若没有自更新守卫拦截，该 set 动作会立即再次触发当前的 effect 执行，导致执行栈瞬间溢出（RangeError: Maximum call stack size exceeded），造成应用崩溃。

🔍 核心原理解析（防拷打）：
1. Vue 在触发依赖通知时，会判断当前准备执行的 effect 是否就是全局正在运行的 activeSub/activeEffect。如果是，且该 effect 没有显式开启 allowRecurse，则会直接跳过本次触发，从而打破死循环。
2. 设计取舍：如果强制抛出错误，会中断正常的边界代码执行；如果不做任何处理，则会导致浏览器崩溃。跳过本次触发是经过权衡后保证应用鲁棒性的最佳方案。
3. 进一步拓展大厂面试追问：在 watch 回调中修改被监听的数据源时，如果需要允许有限制的递归（例如级联表单填充），可以开启特定选项或通过异步调度（微任务去重）来避免同步死循环。`,
        },
        {
          type: 'single',
          question: '`targetMap` 采用 WeakMap 作为第一层容器，最关键的工程价值是？',
          options: ['提高 JSON 序列化速度', '避免 target 被强引用导致内存泄漏', '让 key 只能是字符串', '提升 TypeScript 推导能力'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果 targetMap 使用普通 Map，那么即使业务组件已经被卸载、对应的响应式对象（target）已经不再被任何地方引用，由于 targetMap 作为全局强引用持有该对象作为 key，垃圾回收器（GC）将永远无法回收该对象及其关联的所有依赖树，导致应用随着运行时间增加而发生严重的内存泄漏。

🔍 核心原理解析（防拷打）：
1. targetMap 采用 WeakMap 数据结构，其对 key（即响应式目标对象）的引用是“弱引用”。这意味着，一旦该目标对象在外部没有其他强引用存在，GC 就可以随时将其回收，同时自动清除其在 WeakMap 中对应的键值对。
2. 设计取舍：WeakMap 无法被遍历，也无法获取大小，但这对于只做精确 key 查找的依赖收集系统毫无影响。为了极致的防泄漏 and 自动化内存管理，牺牲遍历性是完全值得的。
3. 进一步拓展大厂面试追问：WeakMap 的 value 是一个 Map（存储属性 key 到 Dep 的映射），这也是强引用。但只要作为 key 的 target 被释放，整条 value 链条都会被 GC 一并回收。`,
        },
        {
          type: 'multiple',
          question: '面试中谈 `watch` 与 `watchEffect`，哪些说法通常成立？',
          options: [
            'watch 需要明确数据源，可拿到新旧值',
            'watchEffect 会自动收集回调中用到的依赖',
            'watch 一定是同步执行，不能调度到微任务',
            '两者都可以返回或提供停止监听的能力',
          ],
          answer: [0, 1, 3],
          explain: `💡 它解决了什么问题：
如果不提供这两种不同形态的副作用监听器，开发者在面对“需要明确知道新旧值”和“需要自动根据回调收集依赖”这两种截然不同的业务需求时，将不得不手写极其繁琐的依赖追踪和手动对比逻辑，降低开发效率和代码可读性。

🔍 核心原理解析（防拷打）：
1. watchEffect 直接将传入的回调函数作为 ReactiveEffect 的 getter，在首次执行时通过读取响应式数据自动完成依赖收集；而 watch 接受明确的数据源（如 getter 函数或 Ref），并包装出一个专属的监控 getter，只在指定源变化时触发回调，从而能够提供新旧值的对比。
2. 设计取舍：watchEffect 开箱即用，依赖收集更智能，但无法获取变化前的旧值；watch 更加精确和可控，支持配置 immediate 和 deep，但需要显式声明监听目标。
3. 进一步拓展大厂面试追问：watchEffect 的副作用清除回调（onCleanup）会在下一次副作用执行前调用，常用于处理异步请求并发时的竞态条件。`,
        },
        {
          type: 'judgment',
          question: '`shallowRef` 的深层属性变化默认不会触发更新，必要时可配合 `triggerRef`。',
          options: ['对', '错'],
          answer: 0,
          explain: `💡 它解决了什么问题：
如果只提供深层响应式的 ref，当我们在处理大型表格数据、3D 模型场景数据或复杂的第三方非响应式对象（如 Monaco Editor 实例）时，Vue 会深度遍历整个对象树并为其每个属性创建响应式代理，这会造成灾难性的初始化性能和巨大的内存浪费。

🔍 核心原理解析（防拷打）：
1. shallowRef 只追踪其 .value 属性本身的修改（即引用级变化），而不会对赋给 .value 的对象进行深度 Proxy 转换。triggerRef 则用于强行手动触发该 shallowRef 绑定的所有下游订阅更新。
2. 设计取舍：放弃了深层的自动化便利性，换取了极致的性能和低内存占用。对于不需要深层驱动视图的巨型数据，这是极佳的选择。
3. 进一步拓展大厂面试追问：在组件库开发中，配合使用 shallowRef 和 triggerRef 可以在需要更新时手动触发一次重绘，避免了不必要的深度 Proxy 开销。`,
        },
        {
          type: 'single',
          question: '在组件嵌套渲染中，父子 render effect 的正确关系应是？',
          options: ['子 effect 永远覆盖父 effect，不需要恢复', '通过保存/恢复前一个 active effect 保证上下文正确', '父子共享同一个 effect 实例', '父 effect 在子 effect 创建后会永久失效'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果不通过栈或父指针恢复前一个 active effect，在父组件渲染过程中渲染子组件时，子组件的渲染 effect 会覆盖全局 of activeEffect。当子组件渲染完成并退回父组件的后续渲染逻辑时，后续读取的父组件响应式属性会被错误地收集到子组件的依赖中，导致父组件状态改变时无法触发自身的重新渲染。

🔍 核心原理解析（防拷打）：
1. Vue 3 在组件渲染或执行 effect 的 run 方法时，会通过保存局部变量 parent 形成一条嵌套链，或者在执行前后分别调用 pauseTracking() / resetTracking() 配合全局的 activeEffect 变量做栈还原。
2. 设计取舍：相比于在渲染每个组件时完全隔离上下文，使用嵌套指针链是一种轻量级且运行极其高效的方案，保证了即使在多层嵌套、动态组件和插槽分发的复杂场景下，依赖收集依然绝对精准。
3. 进一步拓展大厂面试追问：在 SSR 环境下，由于没有真实的 DOM 渲染，渲染 effect 的处理和依赖追踪会有所不同，通常会跳过 effect 的创建以提升服务器吞吐量。`,
        },
        {
          type: 'single',
          question: '以下哪个场景最适合在面试中用来说明“异步校验竞态”问题？',
          options: ['CSS 动画掉帧', '用户名唯一性校验旧请求覆盖新输入结果', '图片懒加载闪烁', '路由 Hash 变化'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果不处理异步竞态问题，在输入框高频输入（如搜索联想）时，后发出的网络请求可能因为服务器响应慢而晚于先发出的请求返回，这会导致旧的、已过时的数据覆盖最新的数据，引发严重的 UI 状态错误。

🔍 核心原理解析（防拷打）：
1. Vue 的 watch 和 watchEffect 回调中提供了一个 onCleanup（或作为回调第一个参数传入的 cleanup 函数）。该清除函数会在下一次 watch 回调重新执行之前，或者在当前监听器被停止、组件卸载时同步调用。
2. 设计取舍：Vue 不在框架内部去实现复杂的网络请求取消（如 AbortController）逻辑，而是提供了一个统一的生命周期钩子，让开发者可以优雅地在闭包中设置过期标志或取消上一次的异步任务。
3. 进一步拓展大厂面试追问：在大厂面试中，通常要求手写一个支持 cancellation 的异步 search 组合式函数，这正是利用 onCleanup 来使上一次的 Promise 失效。`,
        },
        {
          type: 'multiple',
          question: '关于 `reactive` 与 `ref` 的边界，哪些是面试高频正确点？',
          options: [
            'reactive 主要面向对象类型',
            'ref 可包装原始值并通过 `.value` 访问',
            'reactive 变量直接整体替换通常不会影响已有引用绑定',
            'reactive 解构后常需 toRefs/toRef 保持响应式连接',
          ],
          answer: [0, 1, 3],
          explain: `💡 它解决了什么问题：
如果不明确 reactive 与 ref 的使用边界，开发者在编写业务逻辑时极易踩中“整体替换 reactive 对象导致视图失去响应式”、“解构 reactive 丢失追踪”等红线，导致代码混乱且难以维护。

🔍 核心原理解析（防拷打）：
1. ref 底层通过 RefImpl 类的 get/set 访问器属性拦截 .value 读写，因此它可以接收并包装任何原始类型和对象；而 reactive 必须基于 ES6 Proxy 代理对象，无法对原始值进行包装。
2. 设计取舍：ref 在 JS 中书写稍显繁琐（需要 .value），但胜在通用、安全且支持解构中转；reactive 在使用对象时直观、无 .value，但存在整体替换丢失响应式和无法安全解构的硬伤。
3. 进一步拓展大厂面试追问：在大型项目中，为了统一心智模型和防范错误，团队通常会规范“一律使用 ref”或“仅在定义组件内部紧密关联的状态集时使用 reactive”。`,
        },
        {
          type: 'judgment',
          question: '`effectScope` 的价值之一是组件或模块销毁时集中清理多个副作用。',
          options: ['对', '错'],
          answer: 0,
          explain: `💡 它解决了什么问题：
如果缺少统一的副作用回收容器，当我们在状态管理库（如 Pinia 的 Setup Store）或自定义 Composables 中动态创建了大量的监听器（watch）和计算属性（computed）时，必须小心翼翼地手动保存它们的停止函数并在销毁时逐一调用，极易发生遗漏并造成内存积压。

🔍 核心原理解析（防拷打）：
1. effectScope 通过全局变量 activeEffectScope 在其 run 方法执行期间，隐式地把所有新创建 of effect 实例的 scope 属性指向当前 scope，形成一棵 scope 树。当调用 stop() 时，它会递归销毁整棵树上的所有副作用。
2. 设计取舍：这是一种“无感收集”的设计，不需要修改任何 watch 或 computed 的调用方式，即可在作用域内实现自动捕获，大大提高了工程可控性。
3. 进一步拓展大厂面试追问：嵌套的 effectScope 还可以通过配置 detached: true 来使其独立于父作用域的销毁，适用于某些需要跨生命周期持久存在的全局服务。`,
        },
        {
          type: 'single',
          question: '若面试官追问“batch 和 queueJob 的区别”，更准确的回答是？',
          options: [
            '两者完全等价，都是组件队列',
            'batch 在响应式通知层合并 subscriber，queueJob 在运行时调度组件更新任务',
            'batch 只用于 SSR，queueJob 只用于 CSR',
            'queueJob 仅用于 watch，不用于组件渲染',
          ],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果不分清这两个层面的去重与批处理，在分析组件性能瓶颈或调试复杂的异步更新顺序时，就会将“数据变更触发的依赖派发”与“组件实际 DOM 更新的调度”混为一谈，无法进行精准的性能分析或时序控制。

🔍 核心原理解析（防拷打）：
1. batch 是响应式系统（reactivity）底层的机制，主要通过计数器控制让多次属性变更触发的通知在 endBatch 时合并发放；而 queueJob 是运行时（runtime-core）调度器（scheduler）的机制，它将组件的渲染/更新任务（Job）放入一个微任务队列中去重并排序，在微任务阶段统一执行 DOM 渲染。
2. 设计取舍：层级划分清晰。底层 reactivity 可以独立于 runtime 运行，而渲染调度则依赖微任务机制实现高效更新，实现了框架模块间的解耦。
3. 进一步拓展大厂面试追问：在同一事件循环中，如果数据变更了多次，batch 阻止了 effect 的多次无效触发，而 queueJob 保证了即使 effect 触发了，组件的实际 patch 渲染也只会执行一次。`,
        },
      ],
    },
    {
      title: 'Vue3 Diff算法的整体流程详解',
      difficulty: '困难',
      sourceCards: ['Vue 3 Diff 算法'],
      content: vue3DiffMarkdown,
      quiz: [
        {
          type: 'single',
          question: 'Vue 3 diff 性能提升的关键之一是编译阶段引入了什么标记机制？',
          options: ['VNodeHash', 'PatchFlag', 'TreeShakingFlag', 'HydrateMask'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果不引入编译期标记 PatchFlag，Vue 只能像 Vue 2 那样在运行时对新旧 VNode 节点进行全量的、无差别的深度递归对比。即使一个节点上只有一个动态 class，其他所有属性和子节点都是静态的，运行时依然要花费 CPU 资源去遍历和对比它们，这在大型复杂页面中会导致极其低效的 patch 性能。

🔍 核心原理解析（防拷打）：
1. 编译器在将模板编译为渲染函数时，会分析出哪些属性是动态的，并在创建 VNode 时传入一个位掩码（PatchFlag，如 TEXT = 1, CLASS = 2 等）。运行时 patch 阶段通过按位与操作（patchFlag & PatchFlags.TEXT）可以瞬间得知需要比对哪些内容。
2. 设计取舍：将运行时的计算压力分摊到了编译期，使得动态节点的 patch 复杂度与静态模板大小彻底脱钩。
3. 进一步拓展大厂面试追问：手写 render 函数时如果不传入 PatchFlag，Vue 3 就会退化到全量 diff 模式以保证渲染的正确性。`,
        },
        {
          type: 'multiple',
          question: '以下哪些属于 Vue 3 编译时优化手段？',
          options: ['静态提升（hoist）', '事件监听器缓存', 'Block tree 动态子节点收集', '每次渲染都重建全部 vnode'],
          answer: [0, 1, 2],
          explain: `💡 它解决了什么问题：
如果不实施静态提升、事件缓存和 Block tree 优化，每次组件重新渲染时，都会在内存中重新创建成百上千个完全相同的静态 VNode 节点和事件监听函数，这不仅大幅增加了垃圾回收（GC）的频率，还会因为重复对比静态节点和重新绑定事件而拖慢每一帧的响应速度。

🔍 核心原理解析（防拷打）：
1. 静态提升（hoistStatic）将不随状态改变的 VNode 提升到渲染函数外部定义，每次渲染直接复用该对象指针；事件缓存（cacheHandlers）将内联事件包装成一个长期存活的包裹函数，避免每次生成新函数导致元素重新 patch；Block tree 则将动态节点扁平化收集，跳过静态层级。
2. 设计取舍：通过在编译阶段生成更聪明的渲染代码，用“静态复用”和“扁平通道”替代“运行时动态创建与深度遍历”。
3. 进一步拓展大厂面试追问：静态提升的节点如果数量过多（比如超过 20 个），Vue 会将其自动转化为 createStaticVNode 并生成 HTML 字符串，利用浏览器的 innerHTML 性能进行超快速的首次渲染。`,
        },
        {
          type: 'judgment',
          question: '在更新阶段，Block Tree 的目标是让 diff 复杂度更接近“动态节点数量”而非“整棵模板大小”。',
          options: ['对', '错'],
          answer: 0,
          explain: `💡 它解决了什么问题：
如果 patch 的复杂度取决于“整棵模板的节点总数”，在一些包含大量静态文本、图标但只有极个别数据绑定的超长列表或大型表单页面中，任何微小的状态变更都会触发对数千个静态节点的深度递归，导致严重的卡顿和帧率下降。

🔍 核心原理解析（防拷打）：
1. Vue 3 引入 Block Tree 机制。一个 Block 会在其 dynamicChildren 数组中扁平化地收集其内部的所有动态子 VNode（如带有 PatchFlag 的节点）。在更新阶段，直接对该数组进行一维遍历和 patch，彻底跳过静态层级。
2. 设计取舍：打破了传统的虚拟 DOM 树级比对限制。通过在 VNode 上挂载扁平的动态子节点队列，实现 O(动态节点数) 级别的极速更新，但这也要求编译器在生成 block 时进行精准的边界划分（如 v-if, v-for 会生成新的 block 嵌套）。
3. 进一步拓展大厂面试追问：为什么 v-if 会导致 Block Tree 结构不稳定？因为条件分支会改变节点层级，因此 Vue 编译器会把 v-if 节点强制作为 Block 边界，以确保父 block 的动态节点列表结构稳定。`,
        },
        {
          type: 'single',
          question: '在 `patchKeyedChildren` 中，先做“前置扫描 + 后置扫描”的主要目的是什么？',
          options: ['为了生成 source map', '快速跳过头尾稳定区，缩小中间复杂 diff 范围', '为了兼容 IE11', '为了让 key 失效'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果不先进行头尾扫描，在处理列表头尾未变、仅仅是在中间插入/删除/移动了少数元素的常见场景时（如向列表追加一项、分页切换、排序等），Diff 算法会直接进入复杂的乱序对比计算，耗费大量 CPU 资源去构建映射表和计算最长递增子序列。

🔍 核心原理解析（防拷打）：
1. patchKeyedChildren 首先使用两个指针（i 从头往后，e1, e2 从尾往前）进行新旧列表对比。只要 key 和类型相同就直接进行 patch，一旦遇到不匹配的节点立即停止，从而快速缩窄需要复杂 Diff 的乱序中间区域。
2. 设计取舍：这是一种“贪心预处理”算法，在实际工程中，约 80% 的列表变化都属于头尾稳定的局部变动，因此该步骤能以极低的复杂度（O(N)）解决大部分列表更新。
3. 进一步拓展大厂面试追问：前置/后置扫描结束后，如果旧节点全部被扫描完且新节点还有剩余，可以直接判定为新增并执行挂载；反之，若新节点扫完且老节点有剩，可以直接判定为删除并执行卸载，完全避开了乱序算法。`,
        },
        {
          type: 'single',
          question: '`newIndexToOldIndexMap` 中值为 `0` 通常表示什么？',
          options: ['该节点在旧序列中存在且无需移动', '该节点是静态节点', '该新节点在旧序列中不存在，需要新建', '该节点需要立即卸载'],
          answer: 2,
          explain: `💡 它解决了什么问题：
如果在乱序对比阶段无法精准识别出哪些新节点是完全新增的（即在旧节点列表中根本不存在），算法就无法决定是在旧 DOM 的基础上进行移动复用，还是从零开始创建新 DOM，从而可能导致渲染错误或无意义 of DOM 移动。

🔍 核心原理解析（防拷打）：
1. newIndexToOldIndexMap 是一个长度等于新乱序序列长度的数组，其初始值全部填充为 0。在遍历旧节点并填充映射时，存入的值是旧节点索引加 1（oldIndex + 1），以空出 0 作为特殊保留值。因此，如果某个位置的值仍为 0，则明确代表该新节点在旧序列中找不到匹配，必须执行 mount 创建。
2. 设计取舍：为什么不用 -1 而是用 0？因为在 JS 中，Int32Array（或普通数组）在初始化时 new Array().fill(0) 极为高效，且用 0 做判断比 -1 更快捷，节省了底层的内存管理和初始化开销。
3. 进一步拓展大厂面试追问：由于使用了 oldIndex + 1，在后续利用该数组计算最长递增子序列（LIS）时，需要将取出的值减去 1 才能得到真正的旧节点索引。`,
        },
        {
          type: 'multiple',
          question: '在中间乱序区使用 LIS（最长递增子序列）优化时，以下哪些说法正确？',
          options: [
            '位于 LIS 的节点可保持相对顺序，通常不需要移动',
            '不在 LIS 的老节点更可能触发移动',
            'LIS 的主要价值是减少 DOM move 次数',
            '使用 LIS 后就不需要处理新增和删除',
          ],
          answer: [0, 1, 2],
          explain: `💡 它解决了什么问题：
在无序列表重排场景下，如果不计算最长递增子序列，算法将无法得知如何以最少次数的 DOM 移动操作完成重排，可能会导致对几乎所有的 DOM 元素都执行一遍 insertBefore，产生大量的 DOM 重绘和排版开销。

🔍 核心原理解析（防拷打）：
1. 最长递增子序列（LIS）算法能够在乱序映射数组中，找出一条相对位置保持递增的最长节点序列。属于该序列的节点在 DOM 树中的相对顺序是不需要改变的，因此在移动阶段可以保持原地不动。
2. 设计取舍：LIS 计算本身需要 O(N log N) 的时间复杂度，但相比于昂贵的 DOM 移动操作，在 JavaScript 内存中进行少量的算法计算能极大地提升浏览器的渲染效率，这是一笔非常划算的性能买卖。
3. 进一步拓展大厂面试追问：Vue 3 的 LIS 实现使用了二分查找（Binary Search）结合双亲指针回溯（Parent Pointers）算法，从而能够在 O(N log N) 时间内求出最长递增子序列的索引数组。`,
        },
        {
          type: 'single',
          question: '为什么 Vue 在最后阶段常从后向前处理插入/移动？',
          options: ['避免事件丢失', '更容易确定锚点 anchor，减少定位复杂度', '因为前向遍历语法不支持', '放弃 key 校验'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果在 DOM 重排阶段从前向后遍历插入，由于后面的节点尚未确定位置，我们就无法找到一个稳定的、已经挂载到 DOM 树上的节点作为 insertBefore(newNode, referenceNode) 的第二个参数（即 anchor 锚点），这会导致插入操作定位极其复杂且容易出错。

🔍 核心原理解析（防拷打）：
1. 从后往前遍历新乱序序列时，最右侧（最后一个）新节点的右侧邻居必然是已经确定位置的静态节点或已 patch 好的节点。因此，直接以该右侧节点作为 anchor，可以百分之百保证 insertBefore 能够精准地把节点挂载到正确的位置。
2. 设计取舍：从后向前虽然不符合人类直观的阅读习惯，但在双端 DOM 操作中，它天然地提供了一个天然稳定的右侧锚点，大幅简化了多指针定位的逻辑复杂度。
3. 进一步拓展大厂面试追问：在向前移动的过程中，每当处理完一个节点，该节点本身就会成为下一个前驱节点的右侧锚点，从而形成了一条稳固的“向左蔓延”的挂载链。`,
        },
        {
          type: 'judgment',
          question: '首次挂载阶段，静态节点也会被创建；只是在后续更新中通常会被优化跳过。',
          options: ['对', '错'],
          answer: 0,
          explain: `💡 它解决了什么问题：
如果因为静态节点在后续更新中会被优化跳过，就在首次挂载（mount）时也将其忽略，那么整个页面将缺失这部分静态的 DOM 元素，导致页面显示不完整、结构坍塌。

🔍 核心原理解析（防拷打）：
1. 首次挂载阶段（mount）是无中生有的过程，任何节点（无论是动态还是静态）都必须被真实地创建并插入到 DOM 树中。只有当后续进入更新（patch）阶段时，Vue 才会根据静态提升的 VNode 指针判断其未发生改变，从而直接跳过对它的对比和属性更新。
2. 设计取舍：首次渲染注重“完整创建”，更新渲染注重“精确最小化修改”。Vue 3 将两者的策略完美分离，使得静态提升的收益在多次重复更新中达到最大。
3. 进一步拓展大厂面试追问：如果静态节点非常庞大，在编译时会通过 createStaticVNode 生成一个包含 HTML 字符串的节点，首次挂载直接通过 innerHTML 写入，能大幅提升大块静态 DOM 的挂载速度。`,
        },
        {
          type: 'single',
          question: '在 block 优化路径中，`dynamicChildren` 更接近哪种角色？',
          options: ['完整节点树的镜像副本', '只包含动态节点的扁平“快速通道”', '服务端 hydration 的 HTML 字符串', '编译期产物映射表'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果 Block Tree 中没有 dynamicChildren 这一扁平化的“快速通道”，在复杂的组件树更新时，Vue 就必须递归每一层 VNode（包括那些被 v-if、v-for 包裹的复杂树结构），导致大型应用的 patch 性能随着模板复杂度的上升而呈线性甚至指数级退化。

🔍 核心原理解析（防拷打）：
1. dynamicChildren 是一个只在一维数组中存放当前 Block 内所有动态子 VNode 的集合。更新时，外部只需要遍历这个一维数组，并对每个动态 VNode 执行 patch 操作，直接无视了静态节点的父子包裹嵌套。
2. 设计取舍：打破了传统虚拟 DOM 的树状递归限制，用一维线性遍历换取了极致的 runtime patch 效率。但这也要求编译器在生成 VNode 代码时进行高度精确的配合，自动插入 Block 边界。
3. 进一步拓展大厂面试追问：如果开发者使用自定义 render 函数且没有进行 Block 包装，VNode 上的 dynamicChildren 将为 null，此时 Vue 会安全地退化为传统的全量树形 diff，保证渲染万无一失。`,
        },
        {
          type: 'multiple',
          question: '哪些情况更可能导致节点进入“移动”分支而不是“仅更新”分支？',
          options: [
            'key 存在且新索引出现逆序',
            '新旧节点类型相同且位置不变',
            '中段乱序导致 maxNewIndexSoFar 回退',
            '节点在 LIS 中',
          ],
          answer: [0, 2],
          explain: `💡 它解决了什么问题：
如果无法精确判断哪些节点发生了位置重排（逆序），算法就不得不把所有乱序节点全部重新执行一次 DOM 移动操作，或者误判节点不需要移动而导致重排后的视图依然维持旧的错误顺序。

🔍 核心原理解析（防拷打）：
1. Vue 在遍历旧乱序节点并在新序列中定位其索引时，会维护一个变量 maxNewIndexSoFar。如果当前处理的旧节点在新序列中的新索引 newIndex 大于等于 maxNewIndexSoFar，则更新此最大值；如果发现 newIndex < maxNewIndexSoFar，则说明新节点的位置出现了“逆序”，立即标记 moved = true。
2. 设计取舍：这是一种极其轻量且巧妙的逆序检测机制，仅通过一次 O(N) 的数大小比对，就能在不需要任何复杂图计算的前提下，准确判断出列表中是否发生了元素的交叉换位。
3. 进一步拓展大厂面试追问：如果 moved 被标记为 true，在旧节点遍历完毕后，Vue 就会调用最长递增子序列（LIS）算法生成一个保留不动的节点索引数组，以便在后续的移动阶段只移动那些不在 LIS 中的节点。`,
        },
        {
          type: 'judgment',
          question: '没有 key 的列表在复杂重排场景下，通常比有 key 的列表更容易做稳定复用。',
          options: ['对', '错'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果允许对无 key 的复杂乱序列表直接进行复用，由于缺乏唯一标识，Vue 只能采取“就地复用（patch）”策略（即按索引顺序一一强行对比和修改属性）。这会导致本可以被复用的 DOM 节点（如输入框状态、微观局部组件实例）被强行修改为其他元素的内容，引起临时状态丢失、动画错乱甚至渲染崩溃。

🔍 核心原理解析（防拷打）：
1. 没有 key 时，Vue 会默认新旧列表的对应位置节点是“相同的”（即仅对比 VNode 类型）。它会直接在旧 DOM 上就地修改属性。这虽然免去了 DOM 移动的开销，但在重排、插入或删除时，会引发大量原本可以避免的深度 patch 动作。
2. 设计取舍：有 key 模式下，Vue 能够通过 Map 快速建立新旧节点的精确映射，从而实现精准的 DOM 移动和复用。因此，对于任何动态变化的列表，加上 key 都是保证状态正确性的必选项。
3. 进一步拓展大厂面试追问：在某些纯展示、无状态、只做文本替换的简单列表中，不加 key 触发的就地复用反而可能比有 key 时的节点移动具有更低的性能开销，这也是 Vue 默认不强求 key 的原因。`,
        },
        {
          type: 'single',
          question: '为什么 v-for 相关 fragment 常被单独视为一个 block 边界？',
          options: ['因为它只包含静态节点', '因为其子节点数量/顺序更可能变化，需要独立处理', '因为 v-for 不参与 diff', '因为必须关闭 key'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果列表（v-for）内部的动态节点也被直接扁平化收集到父级 Block 的 dynamicChildren 中，由于列表的项数和顺序是动态变化的，在下一次渲染时，父级 Block 的 dynamicChildren 数组长度和顺序将会彻底错乱，导致一维线性 patch 无法按索引正确匹配，渲染彻底崩溃。

🔍 核心原理解析（防拷打）：
1. 为了防止列表项的动态增删破坏外部 Block 的扁平节点队列，Vue 编译器会将整个 v-for 渲染为一个 Fragment，并将其强制标记为一个 Block 边界。这意味着列表内部的动态节点只会被收集到该 Fragment 的 dynamicChildren 中，而不会外溢到外层的父 Block。
2. 设计取舍：通过在 Block Tree 中引入“嵌套 Block”和“Fragment 分区”，成功隔离了列表长度变化对父级扁平通道的冲击，维护了 Block Tree 的整体稳定性。
3. 进一步拓展大厂面试追问：同理，v-if / v-else 分支由于会改变子树的拓扑结构，也被强制编译为 Block 边界，保证每个分支内部的 dynamicChildren 能够拥有绝对稳定的结构。`,
        },
        {
          type: 'single',
          question: '在中段 diff 中，如果某旧节点在 `keyToNewIndexMap` 中找不到对应新索引，应该如何处理？',
          options: ['移动到末尾', '卸载该旧节点', '强制复用到最近节点', '标记为静态并跳过'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果对于在新列表中已经彻底消失 of 旧节点不执行主动卸载，这些过时的 DOM 元素将会永久残留在浏览器页面中，造成 DOM 树的无限膨胀和严重的内存及 UI 混乱。

🔍 核心原理解析（防拷打）：
1. 在乱序对比阶段，Vue 会遍历旧乱序节点，通过其 key 在由新节点构建的 keyToNewIndexMap 中查找对应的新索引。如果查找结果为 undefined（即找不到映射），则明确代表该节点已被删除，Vue 会立即调用 unmount 方法对该旧节点执行卸载、解绑事件并移除 DOM。
2. 设计取舍：通过在扫描旧列表的第一时间进行查找和清理，可以尽早释放不再需要的 DOM 和组件实例，为后续的新节点挂载和移动扫清障碍。
3. 进一步拓展大厂面试追问：为了提高查找效率，Vue 会优先建立 keyToNewIndexMap（O(N) 时间和空间），使后续的查找操作能在 O(1) 的时间内完成，避免了双重循环的低效对比。`,
        },
        {
          type: 'multiple',
          question: '面试里描述 Vue3 diff 优势时，哪些说法更准确？',
          options: [
            '编译时标记 + 运行时定向 patch',
            '静态提升可减少重复 vnode 创建与 patch 成本',
            'LIS 优化目标是减少 DOM move 次数',
            'Vue3 完全不需要 patch children 逻辑',
          ],
          answer: [0, 1, 2],
          explain: `💡 它解决了什么问题：
如果在组件更新时依然每次都为静态内容创建新的虚拟节点（VNode），即使这些节点根本不会发生任何变化，运行时依然要为它们分配内存、进行属性比对。这在含有大量静态描述的页面中，会造成显著的垃圾回收（GC）开销和无谓的 CPU 消耗。

🔍 核心原理解析（防拷打）：
1. 静态提升（hoistStatic）是 Vue 3 编译器的核心机制。编译器识别出完全不依赖组件状态的静态子树，将其提升到渲染函数外部定义为单例。在组件更新触发渲染函数重新执行时，返回的虚拟树中这些静态节点直接复用相同的 VNode 引用。
2. 设计取舍：由于新旧 VNode 引用完全一致（oldVNode === newVNode），在 patch 阶段，Vue 会在一开始就通过全等判断直接判定其无需任何对比并立即返回，从而实现了真正的零成本 patch。
3. 进一步拓展大厂面试追问：这对于内存优化尤其明显。在多实例组件（如列表中的多项）中，所有实例共享同一份提升后的静态 VNode，极大地缩减了内存占用。`,
        },
        {
          type: 'judgment',
          question: '从后向前处理插入时，锚点通常选“右侧已确定位置节点”，这有助于稳定插入逻辑。',
          options: ['对', '错'],
          answer: 0,
          explain: `💡 它解决了什么问题：
如果在移动 DOM 节点时无法确定一个绝对稳定的目标位置参考物（锚点），我们通过 appendChild 或 insertBefore 操作移动节点时，极易破坏其他已经排好序的兄弟节点的相对位置，导致最终生成的 DOM 顺序混乱。

🔍 核心原理解析（防拷打）：
1. 在乱序重排的最后一步，Vue 采用从后往前（倒序）的指针移动策略。对于每个需要插入或移动的节点，其右侧邻近节点的新位置已经在前一步被百分之百确定并处理完毕。因此，直接取右侧节点的真实 DOM 作为 anchor 传给 hostInsert，可以确保当前节点能被平稳、精确地插入到其最终位置。
2. 设计取舍：利用了“倒序处理中右侧必然已稳定”的数学特性，避免了在正序处理时需要动态预估和计算后方节点真实 DOM 的难题。
3. 进一步拓展大厂面试追问：如果当前节点是新列表中的最后一个节点，其右侧没有新节点，那么锚点将为 null，此时底层会自动调用 appendChild 将其追加到父容器末尾，这同样是绝对安全的。`,
        },
        {
          type: 'single',
          question: '当用户手写 render 且缺乏编译器注入优化标记时，更常见的结果是？',
          options: ['自动补齐全部 PatchFlag 并保持同等优化', '部分优化路径收益下降，更依赖通用 diff 逻辑', '直接无法渲染', '自动退化到 Vue2 运行时'],
          answer: 1,
          explain: `💡 它解决了什么问题：
如果当用户手动编写渲染函数（没有编译器的 PatchFlag 等标记）时，Vue 依然强行使用 Block Tree 等优化路径，就会因为找不到 dynamicChildren 而忽略动态属性的更新，或者因为错误的假设而导致页面无法正常渲染。

🔍 核心原理解析（防拷打）：
1. 当开发者绕过编译器手写渲染函数时，生成的 VNode 默认是不带 PatchFlag 和 dynamicChildren 的。Vue 在运行时检测到这一情况，会安全地将整个 patch 过程退化为经典的“全量 diff 模式”（即递归对比整棵虚拟 DOM 树的所有节点和所有属性），以确保数据的变更能够百分之百正确地反映到 DOM 上。
2. 设计取舍：在“性能优化”与“运行正确性”之间，正确性永远具有最高优先级。在缺乏编译期静态分析信息时，退化到通用 Diff 虽牺牲了部分性能，但保证了框架的极致兼容性。
3. 进一步拓展大厂面试追问：如果开发者希望手写 render 同时保留优化，可以手动调用 Vue 导出的 withDirectives、createVNode 并传入对应的 PatchFlag 整数，从而主动告知运行时其具体的动态变化范围。`,
        },
      ],
    },
  ],
};
